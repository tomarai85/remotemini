# 監査 2026-08-08 —— 失われた一覧の作り直し(当てる前に、分母つきで)

## 0. この file が在る理由と、書けなかった事

`.harness/sprint-8-brief.md` §2 の通り、8/08 に回した2本の監査の一覧は disk に残って
いない。**思い出しでは復元できない**(それは復元ではなく発明になる)ので、監査を
回し直して作り直した。番号は元の一覧と繋がらないので新しい採番にする ——
経路側 `R-n` / 体験側 `X-n`。

★方法についての訂正を先に書く。当初この2本は subagent 2体に回す設計だった。
**4回試して4回とも、指定した出力 file を書かずに終わった**(`audit-route` /
`audit-exp` の3世代 + 範囲を絞った4回目)。3回目で方法を変え、4回目で
subagent を捨てて自分で走査した。だから以下の分母は「subagent が読んだ範囲」では
なく、**私がこのセッションで実際に走査した範囲**であり、下に明記する。

## 1. 経路側 `R-n`

**分母 = `rc-backend/src/*.mjs` 全 19 file(6,867 行)を、名前を挙げた5つの危険型で走査。**
走査したのは次の5型で、それ以外は見ていない:
① 同期の子プロセス(`execFileSync`)の時間の上限、② `|| true` / `?? true` 型の
fail-open 既定、③ 要求本文の大きさの上限、④ `JSON.parse` の保護、
⑤ 外部スクリプトへの依存。**行ごとの通読はしていない** —— 型を決めて横断した。

### R-1. 同期の子プロセスに、時間の上限が**一つも**無い(19 file 中、該当 4 経路) —— **当てた(S8-9)**

- **観測**: `execFileSync` を呼ぶ経路は4つ —— tmux 実行器(`makeTmuxRunner` の
  `call`)、`ps` の実行器、`/api/account`、`/api/account/next`。
  4つとも渡している option は `encoding` と `env` だけで、**`timeout` が無い**。
- **事象**: 子が固まると Node の event loop ごと止まる。止まるのはその要求だけでは
  なく**サーバ全体**で、`/health` すら返らなくなる。
- **これは推測ではない**: `server.mjs` 自身のコメントが、別件(e2e の対照が
  12回中12回赤になった件)の原因として「`execFileSync` で event loop ごと止まるので、
  負荷が乗るほど到着が散る」と**既に書いている**。同じ性質を、上限が無い状態で
  本番の hot path(poll ごとの tmux 呼び出し)が踏んでいる。
- ~~**渡米との関係**: 固まった時に効く手が「机で殺す」しかない。8/20 以降その手は無い。~~
  **↑ 誤り。訂正(2026-08-08、当てた後に自分の書類で反証)**: `PRE-DEPARTURE-2026-08-20.md`
  は同じ書類の中で2箇所、復旧手段を「渡米先からでも Blink 経由で ssh して開き直せる」と
  書いている。iPhone から ssh する経路は在る(Blink 自作版、2026-07-30 に実機7通りで検証済)。
  **正しい severity**: 固まっても電話から ssh で殺して開き直せる。効かないのは tailnet 自体が
  落ちた時だけ(同書類が既に「復旧用の ssh も同じ経路なので同時に死ぬ」と書いている)。
  本当の代償は「回復不能」ではなく「**人が気付いて ssh するまで電話が死んでいる**」。
  だから優先順位は上限そのものより**気付く手段**(= 外向きの生存監視)の方が高い ——
  そちらは別項目に起票した。★監査の重大度は、その場の印象ではなく**既に在る回復手段を
  数えてから**書く。書いた時に手元に在った書類を読み直していれば防げた。
- **直し方**: 4経路の option に `timeout` と `killSignal` を足す。値は tmux と `ps` で
  違ってよい(tmux は poll ごとに走るので短く、外部スクリプトは長く)。
- **誰の仕事か**: 私。裁定は要らない。ただし**本番の挙動が変わる**ので単独の節にする。
- **結果(2026-08-08)**: tmux 2000ms / `ps` 5000ms / 外部台本 10000ms、全て `SIGKILL`。
  数字の出し方(電話の 8 秒・echo 予算 1500ms・Codex の帯の**交点**)と、
  上限では塞げない4種は DESIGN §2.59。`run()` と `psSnapshot` は**触っていない** ——
  どちらも既に「判らない」へ倒れる形になっており、要ったのは諦める時刻だけだった。

### R-2. 外部スクリプト依存が2経路、しかも中身が repo の外(R-1 の中で最も深い) —— **当てた(S8-9)**

- **観測**: `/api/account` と `/api/account/next` は `FLEET_ACCOUNT` を同期で叩く。
  この script は repo の中に無いので、**版管理の外の物にサーバの応答性が繋がっている**。
- ~~**R-1 と分ける理由**: 直し方が違う。R-1 は上限を足せば済むが、此処は上限に達した
  時に電話へ何と出すかを決める必要が在る(「口座が読めない」は電話に出る文になる)。~~
  **↑ 誤り。訂正(2026-08-08、実測)**: **電話はこの道を1回も呼ばない**。
  `ios/Sources` / `ios/Tests` / `ios/UITests` を `account` で走査して当たるのは
  `KeychainCredentialStore` の属性名2箇所だけで、道の呼び出しは0。呼ぶのはサーバ自身が
  埋め込んでいる HTML と e2e と書類で、口座の切り替えは spec で v1 の対象外。
  **正しい分ける理由は逆向き**: 電話が呼ばない道だから 8 秒の制約が掛からず、
  上限を長く(10000ms)取れる。そして**本当の怖さは、電話が呼ばない道の hang が、
  電話が頼っている道を巻き添えにする事** —— event loop は1本しか無い。
  ★「外部依存だから電話に出る文を決めねば」は、**呼び出し元を数えずに書いた**。
- **誰の仕事か**: 私。ただし R-1 を当てた後。
- **結果(2026-08-08)**: 両方 10000ms / `SIGKILL`。`--next` の側だけ副作用が在る
  (口座を進める)ので、**時間切れは取り消しではない** —— 台本が進めた後で固まった場合、
  殺しても口座は進んでいる。だから 500 を見て自動で撃ち直す物は作らない。DESIGN §2.59。

### R-3. fail-open 型の既定は **0 件**(緑の観測。分母に対して「無い」と言える)

- `|| true` / `?? true` の grep が 19 file で 0 件。読めない事を「大丈夫」へ倒す
  既定は、少なくともこの形では入っていない。
- ★これを書くのは、監査の一覧が「見付かった物」だけだと**分母に対して何が緑なのか**が
  次のセッションに残らないから。

### R-4. 要求本文の上限は在る(緑の観測)

- `readBody` は既定 64KB で、超えたら `body too large` で切る。上限無しではない。

## 2. 体験側 `X-n`

**分母 = 2026-08-08 に simulator で実際に撮った3枚**(割り込み中 / 打鍵中 / 送信中)。
撮影経路は `ios/UITests/InFlightUITests.swift` の3本で、`xcresult` から取り出して
1枚ずつ**目で見た**。

**分母の外**(= 今回見ていないので、緑とも赤とも言えない): 一覧画面、鍵入力画面、
選択カードが**押せない**時の面、履歴を遡って読んでいる最中の面。

### X-1. 打鍵中、押した鍵のスピナが**伏せられたボタンと一緒に薄くなる**

- **観測**: スピナはボタン label の中に確かに出ていて、押した鍵にだけ出る
  (`spins(key:inFlight:)` は画面まで届いている)。だが `choiceEnabled` が飛んでいる間
  **両方の鍵を伏せる**ので、スピナも一緒に淡くなる。
- **なぜ問題か**: 「どちらを押したか」を持っているのは文ではなくスピナだけ。
  その唯一の手掛かりが、最も見えにくい状態で出る。
- **直し方の候補**: 押した行だけ不透明を戻す / 行の左に印を残す / 文の側に鍵の名を書く。

### X-2. CHOICE 画面に、ほぼ同じ結びの理由文が**2つ縦に並ぶ**

- **観測**: `interruptDisabledReason` と `composerDisabledReason` が同時に出て、
  どちらも「下の選択肢から選んでください」で終わる。電話の幅では2行×2で、
  **選択肢そのものより理由の方が高い**。
- **#28 との関係**: `interruptAllowedOnChoiceScreen` が Yes になると片方が黙るので、
  この項目は**#28 の裁定と束ねて扱う**。単独で文言だけ直すと二度手間になる。

### X-3. 「以前を読む」が灰色の `.bar` の**中**に描かれている(3枚とも) → **当てた(S8-12)**

- **観測**: 転写の続きを読む入口なのに、composer 側の道具に見える位置と背景に居る。
- **当て方**: 位置は動かさず(下端のままが正しい)、`.bar` を composer だけの物に戻した。
  この app で `.bar` は装飾ではなく**所属の宣言**だから(`DESIGN.md` §2.63 —
  帯より下 = 電話が受け付ける面 / 帯より上 = 机から来た転写)。
- **実測(器: 画素)**: 帯の上端 2212→2321(割り込み中) / 1627→1736(確認待ち) /
  地 246→255(送信中)。青いボタンの塊は**1画素も動いていない** = 位置は不変、所属だけが変わった。
- **見張り**: 単体 `bar-is-composer-only` + 負の対照 `bar-material-control.sh`(変異4本)。
  材質は単体でも XCUITest でも読めない(色も材質も取れない)ので、見張りはバイト走査しか
  有り得ず、走査は改名や枝の追加で**静かに的を外す**。だから外れた時に赤が出る事の方を測る。

### X-4. 送信が飛んでいる間、composer は生きていて打ち足せる → **当てた(S8-8)**

- **観測**: 送信中の写真に本文 `ping` が残っていた。残るのは設計通り(分類が
  終わるまで本文に触らない)。問題はその次で、`composerEnabled` は `isSending` を
  見ていないので**30 秒の間ずっと打ち足せる**。そこで成功が来ると `draft = ""` が
  走り、**一度も飛んでいない文字まで消えていた**。
- この監査で唯一、当てた項目。詳細は `DESIGN.md` §2.58。

### X-5. 転写が上詰めで、行が少ない時にボタン列との間に大きな空白が出る

- **観測**: 4行しか無い fixture では、転写の下に画面の3分の1ほどの空白。
  会話の道具としては下詰め(新しい行が下端に来る)が慣習。
- **直し方の候補**: 常に下詰め / 行が画面より少ない時だけ下詰め。
- ★弱い項目である事を書いておく: 分母が「4行の作り物」1つなので、実際の会話の
  長さでどう見えるかは**測っていない**。
- **見送り(2026-08-08、S8-12 の後)。理由を2つとも書く**:
  - **悪化していない事は測った**。X-3 は帯の材質だけを変えて位置は動かしていないので、
    空白の量に関わる寸法は動いていない(上の画素の実測。ボタン列は1画素も動かず)。
    つまり X-5 は S8-12 で**新しく生まれても悪くもなっていない**、元から在る形。
  - **分母は、もう作れる**。`HistoryFixture` に `conversation-long` が既に在り、
    最初の取得で 60 行 / `limit` を上げると 90 行を返す。「4行の作り物1枚しか無い」は
    もう制約ではない。撮るなら `.harness/evidence-*` ではなく scratchpad へ直接
    (`shots.sh` の出力先は日付で固定されていて、repo に未追跡の PNG が落ちる)。
  - 従って**次に触る時は「直すかどうか」ではなく、まず長い会話で見る**。
    4行で気になった空白が 60 行では存在しない可能性が高く、その場合は直す物が無い。

### X-6. 所見ではなく方法の記録 —— 「見付けた欠陥」が**見に行ったら無かった**

- 縮小した写真では、割り込み中の1枚の状態バーの隅にスピナ状の点が見えた。
  切り出して4倍に拡大したら、そこに在ったのは**時計の表示だけ**で、点は無かった。
- 拡大しなければ「画面の隅に迷子のスピナが出る」という**存在しない欠陥**が1件、
  この一覧に残っていた。撮る事と**見る事**は別の作業である、の実例。

## 3. 自分で見つけた分(監査とは別。S8-6 の積み残しを追っていて出た)

### J-1. fixture の会話画面が、送信 / 割り込み / 選択の3経路だけ**本物の client を握っていた** —— **出荷済み(S8-7)**

- **観測(当時)**: `ConversationViewModel` の `init` は4つの client に既定値を持ち、
  既定はそれぞれ本物。`RootView` の DEBUG 分岐は履歴と poll の**2つだけ**を
  fixture に差し替えていて、残り3つは本物のまま。押せば `ui-fixture.invalid` へ
  本当に HTTP が飛ぶ。
- **なぜ欠陥か**: `PollFetchingFixture` の doc が、自分の存在理由の1番目に
  **まさにこれ**(poll を渡し忘れて本物が走っていた)を挙げている。同じ理由が
  書き込み3本には当てられていなかった —— Sprint 4 が poll について直した欠陥が、
  Sprint 5 / 6 / 7 と**3回続けて再発**していた事になる。
- **当時の火の大きさ(誇張しない)**: `ios/UITests` の `.tap()` は3箇所しか無く
  (古い行を読む / 一覧の行 / 戻る)、書き込みの button を押す検査は1本も無かった。
  だから**潜在**だった。
- **効き方**: S8-6 の積み残し「打鍵のスピナを実機で一度も見ていない」の原因が此処。
  押した時の滞空時間が計器ではなく**環境**(DNS が `.invalid` を蹴る速さ)で決まる。
- **出荷**: `ConversationClients`(既定値なしの束)+ `WriteFixture`(60 秒返さない
  作り物3本)。`RC_UI_FIXTURE` の面は1つも増えていない。`DESIGN.md` §2.57。
- **代償として書いてあった「2本目の環境変数」は入れていない** —— 「飛びっぱなし」と
  「即座に失敗」を撮り分ける必要が現時点で無く、入れると
  `ios/tools/ui-fixture-absence-control.sh` の走査対象を広げる義務が付いてくる。

## 4. Codex 指摘由来 `C-n`(この2本の監査とは別の出所)

★分母を汚さない為に別の節にした。§1 の `R-n` は「19 file を5つの危険型で横断した」
掃き出しの結果で、下の項目は**その掃き出しから出ていない** —— S8-9 を書き終えた時に
Codex が別途挙げた物を、私が「上限では塞げない4種」の筆頭として起票した。
`R-5` として §1 に混ぜると、経路側の分母が実際より大きく見える。

### C-1. poll の同時実行が `/healthz` を飢えさせる —— **取り下げ(S8-10、実測で前提が再現せず)**

- **主張(起票時)**: poll を何本も張ると同期の子で event loop が埋まり、
  `/healthz` が飢える。§2.59 で入れた上限は**呼び出し1回**の長さしか縛らないので、
  短い呼び出しが大量に来る形はこの手当を素通りする。
- **↑ 前提が再現しない。訂正(2026-08-08、実測)**: サーバが実際に走る機械(edith)で、
  子プロセスに本物の所要を払わせて測ると、電話1台・喋っている会話1つで
  `/healthz` は **max 98ms / 200 以外 0 回**。会話5・電話2でも **max 100ms**。
  v1 の規模(電話1台)に飢餓は無い。全 11 通りの表は `DESIGN.md` §2.60。
- **測って初めて出た、起票時に見えていなかった事**:
  - 費用の **76〜82% は `ps` 1本**。`psSnapshot` にキャッシュがどこにも無く、
    `resolvePane()` の度に新しく呼ばれる。**poll の本数ではなく `ps` が費用の中心。**
  - 増幅するのは client 数ではなく**会話の活性**(黙 2.8 → 喋 6.4 回/秒)。
  - 空回りは**在るが到達しない**。`POLL_MAX_HELD` 超過で即時返却 → 即座に張り直し。
    会話5・poll 30 で **178.8 回/秒**。`startPolling()` が `guard pollTask == nil`
    なので電話1台では5本目を作れない。**潜在の構造欠陥**として別に起票した。
- **この項目の教訓は主張の当否ではなく、器の側に在る**: 最初の器は偽の tmux の
  区切りを間違えていて「0.1% しか塞がっていない」という**嘘の緑**を出した。
  取り下げという結論は同じでも、**まったく違う理由で「裏付けて」しまう**所だった。
  手当は器に前提の検算を持たせる事(`route` が `tmux` でなければ **exit 2 = 未測定**)。
  その検算が2件目(sh の構文誤り、10/10 再現)を捕まえた。

## 5. この一覧の使い方

- 当てる順は決めていない。決めるのは次の節を起票する時。
- **X-2 は #28 の裁定待ちに束ねる**(単独で当てると二度手間)。
- R-1 / R-2 は本番の挙動が変わるので、他と同じ節に混ぜない。
- X-5 は分母が1枚しかない弱い項目。当てる前にもう一度、長い会話で見る。
- **C-1 は取り下げ済み。**同じ主張が再び出て来たら、まず `DESIGN.md` §2.60 の表を読む事
  —— 生きる条件(同一会話に同時 5 client)を満たしていなければ、測り直す必要は無い。

---

## 6. 二巡目の監査(2026-08-08 夕、並列 6 session)

★**番号が §1/§2 と衝突する**。あちらの `R-n`/`X-n` とは別の出所なので、ここでは
`R2-n`(経路) / `X2-n`(体験)と綴る。同じ番号を再利用すると、後から読む人が
「当てた」と「未着手」を取り違える。

### 6-0. この巡の一番の収穫は、所見ではなく**所見の質**

経路側 4 件のうち **2 件が、既に当てた仕事の再報告**だった。しかも両方とも
「無い」という強い断定で、片方は3台の機械を見たと書いてあった。
**他の session の報告は材料であって観測ではない** —— 当てに行く前に自分で測る。

### 6-1. 経路 `R2-n`

| # | 主張 | 判定 | 根拠 |
|---|---|---|---|
| R2-1 | 外向きの生存監視が何処にも据わっていない | **偽** | 下記 |
| R2-2 | 上限(usage limit)に当たった事を電話に伝える信号が無い | 未検分 | — |
| R2-3 | worker 経路の割り込みが、止まった事を確かめずに成功を返す | **本物・当てた(#36)** | 下記 |
| R2-4 | 同期の子プロセスに時間の上限が無い(4 経路) | **偽** | 下記 |

#### R2-1 が偽である事の実測(2026-08-08 16:15、athenas 上)

- `com.fleet.rc-health-observer` は loaded、`StartInterval` = 600、`runs` = 7、
  `last exit code` = 0。
- 観測者自身のログに `ok` が **15:08 から 16:08 まで 10 分刻みで 7 本**。
- 据え付け時(14:58)に `--inject-fail` で経路を撃った記録が残っており、
  **落ちた時の通知と戻った時の通知が両方とも出し先 rc=0** で出ている。
  = 「叩けている」だけでなく**鳴らす脚まで**実測済み。
- `broken-notified` の印は無し(= いま健全)、`last-ok` は 16:08:02。
- 監査が外した理由の推定: 据え付けが**この日の 14:58** なので、それより前を見たか、
  別の label を探した。**「無い」は時刻を書かないと再現できない主張になる**。

#### R2-4 が偽である事の実測

4 経路すべてに `timeout` と `killSignal` が在る。3 つは呼び出し行に直に書かれており、
4 つ目(`makeTmuxRunner` に渡す `exec`)は `inject.mjs` の `call()` 側に在る。
監査は `makeTmuxRunner` を `inject.mjs` まで追わなかった。これは §1 の R-1(当てた S8-9)
そのもので、**当てた仕事が別の名前で再浮上した**形。

#### R2-3(本物)—— 電話に「止めました」と出るのに、止まった事を誰も確かめていない

- **観測**: tmux 経路は 2026-08-03 に `interrupted` を「押した事」から「止まった事」へ
  結び直し、4 通り(verified / unverified / already-done / null)に分けた。
  **worker 経路はこの直しが渡っていない**。worker の割り込みは、対象が居たかどうか
  だけを見て即座に成功を返す。SIGTERM を送った後、終わるのを待っていない。
- **なぜ効くか**: 電話は「止まりました」と表示する。実際には止まっていない事が有り得る。
  これは Tom が最も嫌う形(**機械が自分の観測でない事を断定する**)そのもので、
  しかも直した筈の欠陥が**片方の経路にだけ残っている**という、一番見付かりにくい残り方。
- **v1 で届くか**: 届く。振り分けは「机に該当ペインが在る → 注入 / 無い → worker」なので、
  机で開いていない会話を電話から止めた時に通る。
- **当て方**: tmux 側と同じ 4 通しにする。「止まった事を確かめられなかった」は
  false ではなく**第三の答え**として出す。
- **2026-08-08 当てた(#36、DESIGN §2.64)**。実際には**二重に嘘**だった —— 監査が挙げた
  ①に加えて、②**Escape は一度も押していない**(この経路は子への停止信号)。表示層が
  両経路に同じ「(Escape)」を書いていたので、動作の名前を `route` で分けた。
  - 3 通し(`verified` / `unverified` / `null`)。tmux の4つ目 `already-done` は
    **この経路には作らない** —— 自力で終わっていれば登録簿から外れるので `null` に畳まれる。
  - 途中で前提が1つ引っくり返った。検査に「ワーカーは画面を撮れないので `stopped` を
    名乗れない」と**注意書き付きで**書いてあったが逆で、ワーカーは子の handle を握っている
    ので画素より強い観測ができる。**誤った前提が検査として固定されていた** = 緑は
    「嘘が守られている」の別名だった。
  - 負の対照 `rc-backend/test/interrupt-honesty-control.sh` が、足したばかりの検査の穴を
    先に見つけた(初回 6 本中 1 FAIL)。競走の順番のせいで「撃った所で死んだ事にする」変異が
    緑のまま通っていた。返り値ではなく**死の約束そのもの**を見る検査を足して塞ぎ、再走 6/6。
  - 実測: `rc-backend` 単体 748/748、負の対照 6/6。

### 6-2. 体験 `X2-n`(全件、自分で読んで検分した)

| # | 主張 | 判定 | 次 |
|---|---|---|---|
| X2-1 | 「机で確認してください」が枝に残っている | **本物・ただし #28 に吸収** | #28 待ち |
| X2-2 | composer が ScrollView の外なので画面外へ押し出せる | 本物 | #28 に束ねる |
| X2-3 | 割り込み/送信ボタンに 44pt の的が無い | **本物** | 当てる |
| X2-4 | 相対時刻に時計が無いので「N 分前」が凍る | **設計通り・欠陥ではない** | 触らない |
| X2-5 | 鍵入力の失敗文が、原因(URL)を名指ししている | 弱い | 保留 |
| X2-6 | 401 で理由を持たずに白紙の鍵画面へ飛ばされる | **本物・旅程で鳴る** | 当てる |
| X2-7 | 版の名乗りが鍵入力画面にしか出ない | 本物 | 当てる |
| X2-8 | 鍵入力の待ちが**無言**(約 16 秒)で、入力欄も止まらない | 本物 | 当てる |

- **X2-1 は #28 に完全に吸収される**。残っているのは「確認待ちの画面では、v1 は電話から
  中断しません。机で確認してください」の枝だけで、#28 が Yes になると割り込みが
  生きるので、この理由文そのものが消える。**先に文言だけ直すと二度手間**。
- **X2-4 は欠陥ではない**。一覧の画面には「この画面には時計を置かない」「§2-2/§3-c が
  ここでの poll を禁じている」と**明記された上で**時計が無い。監査は本当に無い物を
  見付けたが、それは決定であって欠陥ではない。**不在は、意図の記録を読むまで欠陥と呼べない**。
- **X2-6 が体験側で最も高い**。理由は旅程: 鍵が旅先で失効すると(#16 のとおり従量課金の
  API キーで走っており `token-failover` の保護が無い)、電話は**理由も無く白紙の鍵画面**に
  なる。何が起きたか分からないまま、直し方も分からない。ここは机が遠い時にだけ効く欠陥で、
  つまり**渡米して初めて鳴る**種類。

### 6-3. 次に当てる順(理由つき)

1. ~~**R2-3**~~ —— **当てた(2026-08-08 #36、DESIGN §2.64)**。実際は二重の嘘で、
   監査が挙げていない②「Escape を押していない」も一緒に直した。
2. **X2-6** —— 旅程で実際に鳴る唯一の体験欠陥。
3. **X2-3 / X2-7 / X2-8** —— どれも小さい。移動中に片手で押す道具なので的の寸法は効く。
4. X2-1 / X2-2 —— **#28 の裁定待ち**(先に触ると二度手間)。
