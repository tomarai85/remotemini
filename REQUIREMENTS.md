# 移動中スマホ作業 — 要件定義

**状態**: 組み立て中(2026-07-31 に方針転換)。当初「★の節は Tom が埋める」としていたが、
Tom 裁定(2026-07-31 逐語):「こういうものを作ってほしいです、こういうものが欲しいです、
こういうものが要件定理ですっていうのは**もう大前提でめちゃめちゃ話してある**と思うんで、
もう大丈夫だと思いますけどね」— つまり空欄は**聞く物ではなく、過去の会話と .md 群から
出典付きで組み立てる物**。採掘結果 = `research/requirements-mining.md`。
解釈の混入防止は「引用+出典+日付を必ず付ける」で担保する(方式を質問から引用に変えただけで、
2026-07-29 の再発防止の趣旨は維持)。

**品質の裁定**(Tom 2026-07-31 逐語):「どんどん作らない。スピードも大事だけど、
**質をめちゃめちゃ高める方が大事**だし、**正確性**だったりだとかも大事だよ。その機能としてはね」
→ 工程は 調査 → 設計 .md → spec → 実装 → Evaluator+Codex 監査 を省略しない。
設計に関わる物は**全部 .md に保存**し、作業の区切りごとに `WORKLOG.md` へ記す
(コンテキスト悪化で忘れる前にディスクへ、も Tom 指示)。

**なぜ今書けるか**: 2026-07-28 に前版(`spec.md` / `REQUIREMENTS.md`)は破棄されている。理由は
`~/.claude/plans/mobile-work-handoff-2026-07-28/measured-plan.md:3`「測る前に書いたので、測るたびに壊れた」。
これは正しい判断だった。本版は**測った後**に書いているので同じ壊れ方はしない。以下の「制約」は全て実測値。

---

## 1. 目的(Tom の言葉・変えない)

移動中(MBP を開けない時間帯)に、iPhone から作業を進める。見るだけ・投げるだけではなく、
**対話しながらその場で直す**。

Tom の逐語要求: 「**普通の Claude code、Native Claude Code App とおんなじイメージ**」

★**注意**: 2026-07-29 のハンドオフ §0 はこの後ろに「= 生のターミナル。チャット風の再構成ではない」を
付けたが、**これは Tom の言葉ではなく解釈**。2026-07-30 に Tom が訂正済み。
本書では解釈を足さない。「同じイメージ」が具体的に何を指すかは §4 で Tom が決める。

## 2. いつ・どこで使うか

| | |
|---|---|
| 渡米前(〜2026-08-20) | 日本での移動中・外出先 |
| 渡米直後 | **使えない**。edith はスーツケースの中 |
| 渡米後(主用途) | 寮で edith 稼働。Tom は移動が多い。**電話が edith への唯一の扉になる** |

**2026-08-20 が縛るのは「機体に物理的に手が届く最後の期間」であって、機能の納期ではない**
(`project_fleet_us_migration_plan_2026_07_22.md:21,25` = athenas と edith は2台とも米国へ持参、
「物理は日本に居る間のみ可」)。UI は Tailscale 越しの純ソフトなので渡米後でも作れる。

## 3. 制約(全て実測値・2026-07-30)

### 3-1. 画面と入力

| 項目 | 実測 |
|---|---|
| 端末の有効サイズ | **78桁 × 32行**(Iosevka Nerd Font 10)。Source Code Pro 10 では 65桁 |
| Claude Code の UI | 80桁未満で折り返して崩れる。78桁はぎりぎり内側 |
| `claude --resume` の一覧画面 | **78桁に収まる**(枠が76桁で描画されるのを実測) |
| ソフトキーバー | PgUp/PgDn を追加済(**実タップ未確認**)。日本語 IME は WebView 経由で構造上通る(実入力未確認) |

### 3-2. セッション

| 項目 | 実測 |
|---|---|
| Mac のセッション数 | **172** |
| edith のセッション数 | **11** |
| **セッションは機体ごとに存在する** | 電話から届くのは edith 側だけ。Mac の172本は `remote-mini` で持ち出さない限り見えない |
| 現状の到達範囲 | **1本のみ**。`phone-shell` が `tmux new-session -A -D -s work` を強制実行し、セッション名がサーバ側に焼き込まれている。`-D` は他クライアントを追い出す |
| Claude Code 内蔵の一覧 | `--resume` / `/resume`。検索ボックス付き、既定は現プロジェクト、`Ctrl+A` で全プロジェクト、`Ctrl+B` でブランチ絞り込み、`Esc` で取消。`--fork-session` / `-n <name>` もある |

### 3-3. 接続と復旧

| 項目 | 実測 |
|---|---|
| 経路 | iPhone → Tailscale → SSH(鍵は iOS Keychain)→ edith の強制コマンド → tmux |
| 到達時間 | 約6秒 |
| 切断からの復帰 | **自力で戻る**。サーバ側切断から4秒以内に再接続(実測)。背面76秒後の復帰でも張り直す |
| mosh | **使えない**。UDP が Tailscale 越しに通らず60秒で自滅 |
| SSH keepalive | **使えない**。upstream が `keepAliveInterval` の代入をコメントアウトしており(理由: libssh が応答を誤処理)、タイマーが起動しない dead code |
| iOS のサスペンド | ソケットを閉じるが、止まった ssh はそれを知る手段がない |
| 認証 | 長寿命 setup-token。`/login` 不要。2時間毎の自動フェイルオーバー配下(`com.tom.token-failover`) |
| ★罠 | 未信頼ディレクトリで `claude` を起動すると**フォルダ信頼プロンプトが1枚出る**。無操作を謳う経路に人間の操作が挟まる |

### 3-4. 配布

| 項目 | 実測 |
|---|---|
| アプリ | 自作 Blink を Tom の開発者証明書でサイドロード。署名は **2027-06-06 まで**有効 |
| 再ビルド | ソースツリー退避済のため差分ビルド数分 |
| App Store | 使わない(Blink 有料版は使用前に課金要求) |

---

## 4. ★未決 — ここが埋まるまで実装に入らない

### 4-1. 開いた瞬間に何が出るべきか

現状は「固定の1部屋に直行」。Tom の 2026-07-30 の指摘:
「**本来セッションは複数だよね？なんでApp開いた瞬間もうターミナルなの？**」

選択肢(いずれも「開いた瞬間の画面」で並べたもの):

| | 開いた瞬間 | 会話を選べるか | 作る量 |
|---|---|---|---|
| A | 固定の1部屋(現状) | 選べない | 完成済 |
| B | Claude Code の会話一覧 → 選んで入る | 選べる | **ほぼ設定変更**(内蔵機能) |
| C | チャットUI(Home Screen アプリ)。差分カード・承認ボタン | 選べる | 中。サーバ側 HTTP アダプタは稼働中 |
| D | ネイティブアプリ自作(SwiftUI)。プッシュ通知・共有シート | 選べる | 大 |

**Tom の回答(2026-07-31・逐語)**:
「そもそも論として、セッションは1つだけではないから、Claude code native の Remote control の
設計を大きく真似する必要があるんじゃないかな、本来はめちゃくちゃ調査とドキュメントとか、
MDとか、実装とか Coding が必要」

実装側の解釈(1行だけ・Tom の言葉と区別する): 真似るのは Remote Control の**設計**であって
製品の採用ではない — §6 の却下(毎回の Loading / セッション移送不可)は製品への却下であり、
その欠点は自前実装(edith 上・Tailscale 内)で直す対象になる。
→ 方向は C/D 系。A は 7/30 に Tom が自身の言葉で否定済み。
設計の具体は `DESIGN.md`(調査後に作成)で確定する。

### 4-2. 「Native Claude Code App とおんなじイメージ」の合格条件

★前提として知っておくべき事実: **iPhone には Claude Code のネイティブアプリが存在しない**。
Tom が見ていた実物は Mac の `/Applications/Claude.app`(`com.anthropic.claudefordesktop` v1.24012.9)。
電話での公式手段はブラウザの claude.ai/code か、却下済の Remote Control のみ。

**充填(コーパス由来・2026-07-31)**: 操作的な定義は Tom の 7/31 指示(§4-1 の逐語 =
「Remote control の設計を大きく真似する」)が与えた — 合格 = `DESIGN.md` §1 の模倣チェックリスト
(状態ドット付きセッション一覧 / 会話ビューの同期 / 通知から開いてその場で返せる)を満たすこと。
★出典間の食い違いを1件検出: `research/requirements-mining.md` §1 行20 は 7/30 の訂正を
「同じ使用感を**生ターミナルで**得る」と読み、本書 §1 は「生ターミナル」は解釈であり訂正済みと
記す。**最新の Tom の言葉(7/31 RC 指示)が操作的に優先** — 7/31 指示はセッション複数前提の
UI を要求しており、生ターミナル固定説とは両立しないため。

### 4-3. セッションの持ち出しとの関係

電話から見えるのは edith 側の会話だけ。Mac の172本をどう扱うか。

> **★2026-08-14 — Tom が回答。空欄が埋まった。**
>
> 逐語: 「**全部だよ**」(電話から触りたいのはどの仕事か、の問いに対して)
>
> = 電話の一覧は **edith のセッションだけでは足りない**。MBP 側で動かしている
> 実案件も届く必要が在る。下の「充填(コーパス由来+設計提案)」は
> **AI 起草の暫定値であって Tom の答えではなかった** —— Phase 1 のスコープを
> 「edith 上のセッション + 持ち出した分」に絞った判断は、**此の回答で上書きされる**。
>
> ★**2026-07-31 の障害はもう無い**: 当時「両機に同一絶対パスが要る / edith に
> `/Users/tomtim` が無い ⇒ **持ち出せる実案件ゼロ**」と記録されていたが、
> 2026-08-03 に前提を実測して **FALSE** と確定した(`--resume` の探索鍵は起動 cwd から
> 作る slug だけで、transcript 内の `cwd` は照合に使われない)。
> `~/.claude/tools/remote-mini.sh` は写像方式へ書き換え済み = **持ち出しは今日通る**。
>
> → 次にやる事は「持ち出しの機構を作る」ではなく **「既に在る持ち出しを電話から
> 使える様にする」**(§9-2 と同じ穴)。

**充填(コーパス由来+設計提案・Tom veto 可)**: Phase 1 のスコープ = **edith 上の
`entrypoint:"cli"` セッション + remote-mini で持ち出した分**。根拠: 「セッションを機械間で
移送する機構」は却下済みで「移送でなく起動時に読む方式で確定」
(`research/requirements-mining.md` §2)、持ち出しの既存解 = remote-mini。Mac 直接到達は
将来拡張として設計だけ機体非依存にしておく(`DESIGN.md` D5)。
付随する分岐(edith に `/Users/tomtim` を作るか = Tom の sudo 1回)は Phase 1 実装中に再提示。

### 4-4. UI の言語

実物は英語。日本語化するか、英語のままか。

> **★2026-08-17 — Tom が veto。UI は英語。**
> 逐語: 「言語は日本語なのか」「普通こういうのって英語だと思うけど」(v61 を実機で開いて)。
> 下の日本語案は提案どおり veto された。**UI の皮は英語**(一覧・ボタン・状態・断り文)。
> サーバが送る表示文(`display.*`)も英語へ —— 「文面の正本はサーバ」の規約は変えず、
> 正本の言語だけが変わる。診断ログ・WORKLOG 等の内部文書は日本語のまま。

**充填(提案・2026-08-17 に veto 済み・記録として残す)**: 自作 UI の皮(一覧・ボタン・状態表示)は
日本語、という案だった。根拠は間接(Tom への応答言語)で、実物が英語である事の方が強かった。

### 4-5. アカウント切替(採掘で復元された必須要件 — 2026-07-31 追加)

**Tom 逐語(2026-07-28)**: 「俺は Claude Code 複数使ってるから、CodexBar と同じように
アカウントの変更もスムーズにできなければならない」(出典 = 破棄前 spec.md Sprint 4 節、
`research/requirements-mining.md` §1)。
→ 電話 UI に**現用アカウントの表示と切替**が要る。土台は実装済み(edith `fleet-account`
--next / 2h 自動フェイルオーバー)。UI はそれを呼ぶだけ。

---

## 5. 合格条件(2026-07-31 起草 — 出典は各行に)

1. 開くと**セッション一覧**が出る(固定1部屋に直行しない)— Tom 7/30「本来セッションは
   複数だよね？」+ 7/31 RC 指示

   ★**此の条件は2回破れている。2回とも「鍵の入力欄が出た」形。**
   1回目(2026-08-11、#64、DESIGN §2.85)= 電話に既定の接続先を渡す経路が無く、
   人が打つ以外に `AppState` へ資格情報が入らなかった。
   2回目(2026-08-15、DESIGN §2.94)= 種は束に在り金庫も読めたのに、台帳が
   「もう蒔いた」と覚えていて蒔かなかった。**金庫だけが電話を通さずに空になる道**
   (検査の後始末・端末の復元)を勘定に入れていなかった。台帳を反転させ、
   覚えるのは「拒まれた種」だけにした。
   ★2回目の診断が速かったのは `loadStoredCredentials()` に計器を1行置いたから ——
   **画面は4本の道のどれを通ったかを一文字も名乗らない**。値は出さず真偽だけ出す。
2. 一覧から選ぶと**その会話の続き**が読める・打てる — 7/31 RC 指示
3. 「対話しながらその場で直す」が成立(見るだけ・投げるだけではない)— §1 の目的、
   Tom 2026-07-28(`user_work_pattern_mobile.md`)
4. Claude が入力待ちで**電話に通知**、開いてその場で返せる — measured-plan §4-3 継承

   **2026-08-12 に通した。** `.harness/spec-native-shell-2026-08-05.md` は此れを
   「wildcard provisioning profile は push entitlement を運べない」を理由に v2 候補へ
   落としていたが、**其の理由は今も真のまま、要件は満たせる** —— 此の条件は APNs を
   名指ししていない。

   実測(2026-08-12、edith): 通知の鎖は**既に全部繋がっていた**。`~/.ntfy-topic` が在り、
   `Notification` hook が `~/fleet-tools/ntfy-notify.sh` を指し、dry-run が通る。
   欠けていたのは「開いてその場で返せる」の**開いて**の部分だけで、通知を押しても
   ntfy のアプリが開くだけだった。

   足したのは2つ: 通知に `Click: remotemini://`(edith 側、`ntfy-notify.sh`)と、
   アプリ側の URL scheme(`ios/project.yml` の `CFBundleURLTypes`)。**APNs の
   entitlement は1つも要らない** —— 配送は ntfy のアプリが行い、電話側の scheme は
   「押された時に何を開くか」だけを担う。

   ★CONTENT-FREE は破っていない: 行き先は会話 id も path も持たず、アプリを開くだけ。
   ntfy の無料枠では topic が事実上パスワードなので、URL に何かを載せた瞬間
   「topic を当てた者が Tom の作業対象を知る」に変わる。

   ★焼き上がった `.app` に scheme が在る事は `ios/tools/build.sh` が実機ビルドの度に
   確かめる(単体検査は plist を選べないので、`project.yml` に書いた事は「焼けた事」の
   証拠にならない —— `CFBundleVersion` と同じ理由の同じ場所)。

   ★**残る Tom ゲート**: iPhone に ntfy を入れて topic を1回購読する事。物理デバイスの
   操作なので私からは動かせない。購読までは通知が届かない(鎖の最後の1段)。
5. 移動中の切断(Wi-Fi↔セルラー・ロック)を跨いで会話が失われない — measured-plan §4-4/6 継承
6. **Loading で待たされない**(RC 却下理由 1: 「connecting」で作業が止まる、Tom 2026-06-13)。
   数値目標は実装時に実測で置く(起草時の実測: `-p --resume` TTFT 2.3〜3.2秒 = 対策必須)

   **2026-08-06 実測で数値を置いた。** その 2.3〜3.2 秒は `-p --resume` の数字で、この設計は
   それを使っていない(常駐 tmux + `capture-pane` に替えた)。実測し直した内訳:

   | 測った物 | 実測 | 出所 |
   |---|---|---|
   | 折衝された版 | **HTTP/2** | Jervis → tailnet → edith |
   | 同一接続の使い回し | **10.6ms**(TLS 0.000) | 同上 |
   | 新規接続1本(TLS 込) | 45〜52ms | 同上 |
   | tailnet の経路が冷えていた1回目 | **870ms**(1回だけ) | 同上 |
   | server `/api/sessions/<id>/history?limit=50` | **1〜5ms** | edith の loopback |
   | server `/api/sessions`(41本) | 38〜65ms | 同上 |

   置く目標(この2本だけを見る。中の処理時間は上の通り誤差):
   - **会話を開いて最初の1画面まで ≤ 200ms**(実測 15〜55ms = 一覧を先に引いた後。会話画面は
     `GET /history?limit=50` の1本だけを待って `ProgressView` を抜ける)
   - **アプリ起動後の最初の1本 ≤ 1.5秒**(実測 870ms + 65ms。経路が冷えている時の1回だけ)

   ★HTTP/2 が効いている事の意味: 20 秒の long-poll が刺さったままでも送信は同一接続を
   多重化するので、**打ち込みが握手をやり直さない**。HTTP/1.1 だったら poll が接続を1本
   占有して送信毎に ~870ms を払う所だった(合格条件3「その場で直す」に直に効く分岐)。

   ★**測っていない事**: 上は全て Jervis(同一 LAN の Mac)からの数字で、**電話の数字ではない**。
   電話はセルラーの往復が乗り、端末が寝た後は Tailscale のトンネル復帰も乗る。
   経路が冷えていた 870ms が電話で常態化するかは実機でしか判らない = Tom の手元でしか測れない。
   ★**行き先を訂正した(2026-08-07)**。此処は元「§5-9 の実機脚」と書いていたが、
   §5-9 は**期限性**であって実機脚ではない = **存在しない節を指していた**。
   正しい行き先 = `HANDOFF-NEXT-SESSION.md` §4 の **8-a**(見るだけ・Yes/No。上の 4 / 7 と
   同じ1回の電話で片付く形にしてある)。読む側の待ち時間を 8 秒へ縮めた判断
   (`BackendSession.interactiveTimeout`)が、**電話の経路でも成立するか**を裁く脚。
7. **所有して拡張できる**(RC 却下理由 2: vendor-closed、Tom 2026-06-13)— 全構成要素が
   Tom の機体上・コード改変可能
8. アカウント切替が UI からスムーズにできる — §4-5(Tom 2026-07-28 逐語)

   **2026-08-12 に出荷した。** サーバ側(`GET /api/account` / `POST /api/account/next`)は
   前から在ったが、電話から**一度も呼ばれていなかった**(`src/server.mjs` が自ら
   「native app から1回も呼ばれていない」と書いていた)。足したのは電話側:
   `ios/Sources/Core/AccountClient.swift` / `Screens/Shared/AccountViewModel.swift` /
   `Screens/Shared/AccountBar.swift` / `Core/AccountFixture.swift`。

   ★**「スムーズに」を2タップにした理由**: 切替は艦隊全体の状態を変え、電話は歩きながら
   親指で触る器。確認を挟まないと、ポケットの中の接触で口座が動く。同じ場所で2タップは
   スムーズだが、誤って動く口座はスムーズではない。

   ★**設計上の非対称(サーバの注釈が根拠)**: `fleet-account --next` は時間切れになる
   **前に**口座を進め終えているので、500 は「口座が動かなかった」を意味しない。だから
   失敗しても**撃ち直さず、現在地を読み直す**。撃ち直す実装は二段進めて一段失敗したと
   報告する。検査 = `ios/Tests/Core/AccountClientTests.swift` の
   `testAFailedSwitchIsNotRetried`、`ios/Tests/Screens/Shared/AccountViewModelTests.swift` の
   `testAFailedSwitchRereadsTheAccountRatherThanAssumingItDidNotMove`、
   サーバ側の契約 = `rc-backend/test/account-routes.test.mjs`(陰性対照3本)。
9. 期限性: 渡米 2026-08-20 は「物理アクセスの最終日」であって機能納期ではない
   (UI は Tailscale 越しの純ソフト = 渡米後も作れる)— REQUIREMENTS §2

---

## 6. 非目標(決定済み・蒸し返さない)

| | 理由 |
|---|---|
| mosh | UDP が通らない。実機で確定 |
| `ServerAliveInterval` の有効化 | 失敗時に teardown しない実装なので復旧機能にならず、既知の libssh バグだけを買う |
| Termius | Tom が過去に使って嫌だった |
| App Store 版 Blink | 使用前に課金を要求される |
| Remote Control | Tom が明示的に却下(毎回の Loading / セッション移送不可) |
| `SideloadFix.dylib` | SSH 秘密鍵を持つアプリに未検証の第三者バイナリを埋めない |

## 7. 構成要素の在り処

| | |
|---|---|
| iPhone アプリの再現キット | `~/Infra/blink-selfbuild/`(build.sh + パッチ10本 + edith 側ツール) |
| edith 側の入口 | `edith:~/fleet-tools/phone-shell`(強制コマンド)/ `claude-work` / `fleet-account` |
| edith の認証レーン説明 | `edith:~/fleet-tools/README-AUTH-LANES.md` |
| 持ち出し/戻し | `~/.claude/tools/remote-mini.sh` + Skill `/remote-mini` |
| 実装の経緯・証拠 | `~/.claude/projects/-Users-tomtim/memory/project_blink_selfbuild_2026_07_30.md` |
| 過去の計測 | `~/.claude/plans/mobile-work-handoff-2026-07-28/measured-plan.md` |

---

## 9. Tom の実機フィードバック(2026-08-14、v56 を実際に触っての逐語)

★此処が §5 の合格条件に**無かった**物。合格条件9本は全部「機能が在るか」で、
「**画面から何ができるか**」「**幾つの画面に分けるか**」を1本も測っていなかった。
2日間の「論外」の構造そのものなので、要件として昇格させる。

### 9-1. 一覧から要らない会話を消せない(逐語:「俺の App 側からいらないセッションを選択して消去することのできない問題があるね」)

> **★2026-08-14 追記 — Tom の回答と、私の側の根本原因**
>
> 消し方の逐語:「**これは1かなぁ、最初から MBP に在った物なら明らかに1**。でも2も
> あるといいかも。**ただここは本当にこの機能が必要なのか相談かな**」
> = 既定は **①一覧から消すだけ**(記録は残す)。②本当の削除は「あると良い」止まり。
>
> ★**そして Tom の「本当に必要か」が正しい。** 同日の実測: 一覧 43 件は
> **1件残らず私の検証残骸**だった(「秒という単位の歴史」×14、「1+1 は?」、
> 折り返し確認 …)。**Tom の仕事は0件**。つまり此の機能が要ると見えた原因は
> 「消す口が無い」ではなく **「私が2週間ゴミを捨て続けた」**。
> 退避先 = edith の `~/.claude/projects-attic/rc-test-20260814-155914/`(消していない)。
> → 優先度を下げる。**先に直すのは私の捨て方**(検証用の会話を残さない)。

現状: 一覧は**読む専用**。会話の削除・保管(archive)の口が電話に1つも無い。

実測(2026-08-14、分母つき): edith の一覧 **43 件中 43 件**が私の検証残骸。
うち **cwd のディレクトリごと消えていて機械的に判る物が 15 件**(`/private/tmp` 9 +
`/Users/Shared/dev/roundtrip` 6)。残り **28 件は cwd が実在する**ので、
**機械には Tom の仕事と区別が付かない**。

★此の 28/43 が「自動で隠す」を否定する。同日、`cwdIsGone`(cwd ごと消えた会話を
一覧から落とす絞り)を実装したが**撤回した** —— 設計上の理由は DESIGN §2.88
(既に `blocked.mjs` の `cwd_missing` の文面 + 409 が同じ問題を「見せて断る」で解いていた)、
実測上の理由は此の数字で、絞りが取れるのは残骸の **15/43 = 35% だけ**。
→ 電話から**人が選んで**保管する口が要る(既定は「一覧から外すが file は残す」)。
  自動判定に寄せない。**捨てたのが誰かを機械は知らないが、Tom は知っている。**

### 9-2. MBP へ戻す機構が電話から触れない(逐語:「MBP に戻す Skills って作ったっけ?Skills だけでなく機能とかも」)

事実: `~/.claude/tools/remote-mini.sh out|back` は**存在し、往復も実証済み**
(2026-07-29、未コミット編集 + 会話文脈が MBP→edith→MBP を生き延びた)。
欠けているのは**電話からの口**。今は MBP のターミナルでしか叩けない = 移動中に
「これは MBP へ戻す」が押せない。
→ サーバに `out` / `back` を撃つ道と、会話の行から選べる UI が要る。
★危険度: `back` は作業木を動かす = 取り返しの付きにくい操作。押す前の確認と、
  「今どちらの機体に居るか」の表示が要る(押せる事より、状態が読める事が先)。

### 9-3. アカウント切替が CodexBar と違う(逐語:「CodexBar からかなり離れていてシンプルな矢印形式で構築されているのがちょっと気に食わない」)

現状: `POST /api/account/next` = **次へ回すだけ**。どの口座が在るのか一覧できず、
狙った口座を選べない。CodexBar は**一覧から選ぶ**形。
→ 口座の**一覧**を返す口(サーバ側の `fleet-account` が持っている情報)と、
  選んで切り替える UI が要る。「次へ」は一覧の中の1操作に格下げ。

**実装済み(2026-08-15)**。設計と実測は DESIGN §2.89。要点だけ:
線に候補が乗っていなかったのが正体で、`GET /api/account` は人向けの6行を `account` の
1鍵に入れて渡していた。`src/account.mjs` の `parseFleetAccount` で構造へ均し、
`POST /api/account/select` を新設。矢印は**退避路として残す**(一覧が読めない時に
唯一押せる手なので消さない)。選べない行(トークン欠け)は一覧から**消さず**、
押せなくして理由を日本語で置く —— 消すと「そんな口座は無い」に見える(§2.88 と同じ判断)。
★台本 `fleet-account` 自体は別レーンの資産なので触っていない。読む側だけを持つ。
  台本に `--json` を足す方が真っ当だが、**それは持ち主の判断**なので提案に留める。

### 9-4. 1画面主義をやめる(逐語:「Mikan の時みたく全て1つの画面で収めようとしなくてもいいのでは。複数の画面やらなんやらを作った方がいい」)

★これは 9-1〜9-3 の**上位の指示**。今の設計は「一覧」と「会話」の2画面しか無く、
口座も送信待ちも計器も**全部その2画面に押し込んで**いる。だから会話画面の下に帯が
3段積み上がり、一覧の行に内部語が並ぶ。
→ 画面を割る: 設定 / 口座 / 機体の移動 / 計器(デバッグ)は**別画面**へ。
  会話画面は会話だけ、一覧は一覧だけにする。

**1枚目を割った(2026-08-15)**: `ios/Sources/Screens/Settings/SettingsView.swift` を新設。
口座(現用 / 候補一覧 / 断り理由 / 退避路)と接続(机の在り処 / 版)を其処へ移し、
一覧の右上のバーは**名乗りと入口の2つ**に絞った。設定画面に会話の操作は置かない ——
何でも屋になると同じ所へ戻る。
残り: 機体の移動(§9-2)と計器はまだ画面を持たない。会話画面の帯3段は手付かず。

### 9-5. ★アプリの1枚目が URL 欄と鍵欄のフォーム(逐語・**2回言われている**)

> 2026-08-11:「**最初から論外だよ。なんで俺が、値を埋めないといけない形になってるの？**」
> 2026-08-15:「**そもそも最初にこういう画面なのが論外なんだけど気づいているかなぁ、、、？
> なんかなんで一向に設計が変わらないのか不思議だったんだけどこれはなんで何？**」
> → 直後に「**論外、もうお前は終わりだ、Handoff をだせ**」。

★**此の項目は 8/15 まで REQUIREMENTS に1行も無かった。** DESIGN の註釈と
`~/.claude/.../method_a_ban_without_a_replacement_route_lands_on_the_user.md` には
書いてあったのに、**要件になっていないから作業列に入らず、4日間 1px も動かなかった**。
9-1〜9-4 と同じ格で此処に置く。

現状: `ios/Sources/RootView.swift` の通常経路の1枚目は
`Sources/Screens/KeyEntry/KeyEntryView.swift`(125行)= **URL 欄 + 鍵欄 + 断り文**。
経緯: 「サーバ名を追跡ファイルの Swift に焼かない」という禁止を1本置いた時、
**其の禁止が塞いだ供給経路を引き直さなかった**ので、穴埋めが Tom の手に落ちた。
`Sources/Core/Provisioning.swift`(8/11)が `Info.plist` から鍵と tailnet 名を読んで
Keychain へ蒔く筈だが、**Tom の画面には未だフォームが出ている**。

→ 直す時に (a) 本物の初回起動でも入力欄に落ちるのか (b) fixture の面だけか、を
  分けて実測する事(band-aid 防止)。**どちらでも設計のやり直しは要る。**

> **★2026-08-16 — (a)/(b) を分けて実測した。前提が1つ偽だった。全文は `DESIGN.md` §2.100。**
>
> | 問い | 観測値 | どう取ったか |
> |---|---|---|
> | (a) 本物の初回起動 | **入力欄に落ちない。一覧に着く** | 器を `simctl erase` して金庫を空にし、`build.sh --sim-app`(種を刻む経路)で焼いた束の初回起動。計器 = `seed path: bundled=true storeReadable=true rejected=empty resolved=true`。画 = `.harness/evidence-2026-08-16/` |
> | (b) fixture の面だけか | **そう。しかも構造的にそうなる** | 8/15 に Tom へ見せた画を出した束の `Info.plist` に `RCBaseURL` / `RCAPIKey` が**無い**。`shots.sh` は `build.sh` の刻む段を通らず、必ず `RC_UI_FIXTURE` 付きで起動する |
>
> = 8/11 の指摘は本物の欠陥で、その日に直っていた(`Provisioning`)。**8/15 に
> 「一向に設計が変わらない」と見えたのは、私が見せた画が製品を映していなかったから。**
>
> **残っていた本物の欠陥** = 資格情報が無い時の面が「**なぜ此処に居るか**」を
> 一文字も名乗らない事。道は4本(種が無い / 種が拒まれた / 金庫が読めない /
> 手で入れた鍵が拒まれた)在り、次の一手が全部違うのに、全部同じ白紙に落ちていた。
> → `Sources/Screens/KeyEntry/DisconnectedView.swift` を新設。1枚目は**理由 + 主行動1つ +
> 退避路**で、打つ欄は其の奥へ移した(消していない —— 机が鍵を回したら手入力が唯一の道)。

---

### 9-0. 合格条件 —— **Tom が画面を見て判る形**(★此れが無かったから §9 は作業列に入らなかった)

此の repo の作業列は「機械の検証器を書けるタスク」しか受け付けない。対照81本 /
e2e 285本 / 単体 827本 は全部スクリプトが緑と赤を出す。対して「最初の画面が
間違っている」には**走らせて赤になる物が存在しない**。だから毎周期、測れる仕事に
押し出される。担当は進捗が出ていると感じ続け、**Tom が見る面だけが止まる**。

**規約: 検証器が書けない事は後回しの理由にならない。Tom の目が検証器。**
画面を1枚焼いて Tom に見せるのが verify であって、緑の数ではない。

| 項 | 合格条件(Tom が画面で観測する) | 状態 |
|---|---|---|
| **9-5** | **アプリを開いた1枚目に URL 欄も鍵欄も出ない。Tom は値を1つも埋めない。** | **実装済 / Tom の目視待ち**(2026-08-16。実測は下の枠) |
| 9-4 | 会話画面の下に積まれた帯が **3段 → 1段以下**。計器と機体の移動が**別画面**に在る。 | **実装済 / Tom の目視待ち**(2026-08-16。常設状態帯1枠 + 計器は設定「計器」節 + 行の言葉は人間語) |
| 9-2 | 会話の行から「MBP へ戻す」が押せ、**押す前に「今どちらの機体に居るか」が読める**。 | **実装済 / Tom の目視待ち**(2026-08-16。行バッジ + 依頼 + requests --run。当時の実測は edith 相手で、その機体は 2026-08-20 に譲渡済。**2026-08-27 に friday で鎖を測り直した**: 依頼を置く → MBP が検知 → `--run` で作業木と記録が戻る → RETURN-REQUESTED 消滅・RETURN-STATE 記録、まで実往復。宛先変更で rc-backend と checkout が同一機体に揃った) |
| 9-1 | 一覧の任意の1行を **Tom が選んで**外せ、外れた事が画面で判る(edith 側の file は残る)。 | **実装済 / Tom の目視待ち**(2026-08-16。swipe/長押し + 設定「保管した会話」で戻せる) |
| 9-3 | 設定画面に口座の一覧が出て、**狙った口座を選べる**(矢印は退避路として残る)。 | 実装済 / Tom の目視待ち |

★**順序を変えるな。9-5 が最上位**(Tom が2回言い、4日間動かなかった項目)。
§9 が1件でも残っている間、対照 / 二重 / 競合の検体には手を出さない。計器は §9 が空の時だけ触る。
