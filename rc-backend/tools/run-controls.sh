#!/bin/bash
# 手元で回す**対照**を全部まとめて回す。
#
# なぜ要るか(2026-08-02): 対照が6本在るのに、それを**まとめて回す物が無かった**。
#   実測した参照元:
#     env-death-controls.sh      → tools/verify-on-edith.sh が呼ぶ(edith 上)
#     phone-window-controls.sh   → 同上
#     pii-controls.sh            → 書類に名前が在るだけ
#     verify-script-controls.sh  → 書類に名前が在るだけ
#     mutation-target-controls.sh→ 書類に名前が在るだけ(pre-commit が回すのは**検査**であって対照ではない)
#     mutation-run-live-controls.sh → **参照 0 件**
#   対照は「検査が壊れていないか」を見る物なので、**誰も回さない対照は対照ではない**。
#   検査そのものが常に緑を返す病気(DESIGN §2.18-10)を見つける唯一の目がこれなので、
#   置き場所を1つに決めて、書類からはここを指す。
#
# edith 上でしか意味が無い2本(実 tmux / 実 launchd 相当)は既定では回さない。
#   `--all` を付けると回す(edith 上での `verify-on-edith.sh` 経由が本来の道)。
#
# 終了コードの扱い: 0=緑 / 1=赤 / **2=測っていない**(変異の走行中など)。
#   2 を緑に丸めない —— 「測れなかった」を「異常なし」と読み替えるのが一番危ない。
#
# ══ ★対照を**新しく書く**時に必ず読む2行(ここに置く理由 = 判断する場所に置かないと効かない)══
#   (1) **入力は本物の生成元から取る。** 手で書いた入力を食わせた対照は、
#       「入力の形についての自分の思い込み」を検証できない —— 思い込みごと緑になる。
#   (2) **直したら、直す前の版で対照が赤になるか個別に見る。** 赤にならない対照は
#       その欠陥について何も測っていない。「守りが緑」と「守りが効く」は別。
#       ★これを**回す物**が `tools/prove-all-controls.sh`(1本だけなら `prove-control.sh`)。
#         規則を書くだけでは効かなかった —— 人が手で思い出して回す限り毎回はやらない、
#         というのが (1) が同じ形で再発した理由そのもの。継ぎ目は毎回**探して**いるので
#         対照を新しく書いても一覧に足す手間は要らない(足し忘れが起きない造りにした)。
#         変異走行中は測れないので断る。`--dry` なら対象だけ出す(実行しない)。
#   経緯: (1) は env-death の対照で一度学んだのに、8/02 に `deploy-dirt-controls.sh` で
#   **同じ形で再発**した(手書きの porcelain 行を食わせて 13/13 緑 → 実物で偽陰性)。
#   前回の是正が「その対照1本を直す」で終わっていたから、次に対照を書く私は規則を読まずに
#   同じ穴を掘れた。だから規則を**呼び口の頭**に移した。詳細 = DESIGN §2.18-10 (15)(16)。
set -uo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT" || exit 1

ALL=0
[ "${1:-}" = "--all" ] && ALL=1

# ★束ねて回す側だけが「空くまで待つ」を立てる(既定は 0 = 即断る)。
# 掛け場所は `xcodegen generate` を撃つ台本 1 本ずつなので、待つ相手の最長は
# `ios/tools/signout-notice-control.sh` の実測 734 秒。1800 秒はその倍以上。
# 手で 1 本叩いた時は既定のまま = 「今 build.sh が握っている」と名指しで断られる。
# 何故待たせるか: 40 分走った掃きが、途中で誰かが build を始めただけで赤くなるのは
# **偽の赤**の作り直しでしかない(2026-08-15 に 9 本作った)。
export RC_XCODE_TREE_LOCK_WAIT_S="${RC_XCODE_TREE_LOCK_WAIT_S:-1800}"

LOCAL_CTLS=(
    test/design-supersede-world-controls.sh   # ★2026-08-04 に此処へ入れた。世界の見分け
                                              # (designOrSkip)の対照。差し替え口だけ在って
                                              # 使う側が居らず、其処が実際に配備を止めた
    test/mutation-run-live-controls.sh
    test/mutation-target-controls.sh
    test/orphan-instrument-scan-controls.sh  # ★2026-08-07。「対照は在るのに撃つ者が居ない」
                                             # を挙げる道具の対照。使い捨ての小さな repo を
                                             # 作って其の中だけで壊す(本物の木は触らない)。
                                             # 未測定(2)を赤にも緑にも丸めない事まで測る
    test/pii-controls.sh
    test/prove-control-controls.sh   # 判定する道具そのものの対照。これが壊れると
                                     # 全ての対照の判定が静かに嘘になるので、砂場の
                                     # repo(RC_ROOT)で測定経路まで通す
    test/prove-all-scope-controls.sh # ★2026-08-05。prove-all が **どの dir を探すか**
                                     # の対照。継ぎ目は毎回探すのに探す範囲が手書きで、
                                     # .harness/ と ios/tools/ の 5 本が視野の外に居た。
                                     # 縮める方向と伸ばす方向の両方を持つ(伸ばす方が本命 —
                                     # 生きた導出と「今の値と等しい定数」を見分けるのは此方)
    test/verify-script-controls.sh
    test/deploy-dirt-controls.sh
    test/session-guard-controls.sh   # ★2026-08-05 に此処へ入れた。電話側の HTTP が
                                     # 転送を拒否するセッション以外を通っていないか、の検査の対照。
                                     # Sprint 1 の Evaluator が見つけた欠陥(既定値だけが守られた
                                     # セッション = 呼ぶ側が素の URLSession を渡せば N5 が消える)の
                                     # 2段目の守り。型で縛る①は既存の client にしか効かず、
                                     # Sprint 2-6 が足す**新しい** client には一切かからないので、
                                     # 綴りの側から見張る。live の木には足すだけ、実測1秒未満
    test/wire-shape-controls.sh      # ★2026-08-05 に此処へ入れた。本番の応答の**形**だけを取る
                                     # 道具(`tools/wire-shape.mjs`)が、中身を伏せるかの対照。
                                     # 一覧には会話の題・直近の発言・作業 dir が載るので、
                                     # 伏せ方が壊れても出力は綺麗に見える = 事故の後にしか判らない。
                                     # 網も鍵も要らない(道具の `-` 口を使う)。実測1秒未満
    test/no-linerefs-controls.sh     # ★2026-08-05 に此処へ入れた。検査の**届く範囲**を測る。
                                     # 電話側の木(`ios/`)に行番号を1件植えて赤になるかを見る
                                     # ので、走査が届いていない事は緑で隠せない。実測1秒未満、
                                     # live の木には**足すだけ**で既存 file には触らない。
                                     # ★部分木(変異の作業コピー)でも緑である事を測る項が本体 ——
                                     # 此処が赤い造りだと変異走行の**全件**が「検出」に化ける
    test/disposable-session-controls.sh
                                     # ★2026-08-05 に此処へ入れた。DoD 9行目を閉じた計器
                                     # (`tools/disposable-session.mjs`)そのものの対照。守る本命は
                                     # **破壊側** —— `down` は tmux を殺し file を消すので、名前の
                                     # 検査(`rc-e2e-<数字>` 以外は畳まない)が緩めば Tom の実
                                     # セッションを殺せる。だから「殺さない事」を戻り値ではなく
                                     # **行為**で測る(偽 tmux に argv を記録させ `kill-session` が
                                     # 0 回である事を数える)。⑯ は検査を外した写しで ⑦ が
                                     # **赤になる**事まで見るので、対照が空振りしている状態を緑で
                                     # 隠せない。砂場は mktemp、実物の `~/.rc-backend` にも
                                     # `~/.claude` にも触らない。実測1秒未満
    test/child-reaping-controls.sh   # 実際に変異台本を起こして殺す。実測10秒だが e2e の子が
                                     # 上がるまで待つので状況で伸びる(上限90秒で測定不成立)
    test/health-observer-controls.sh # 本物の HTTP サーバを立てて probe を測る。実測3秒
    test/example-artifacts-controls.sh # 見本(plist / conf)が据える前に壊れていないか。実測1秒
    test/gui-run-controls.sh         # edith の launchd の中で走らせる計器が、終了コードを
                                     # ちゃんと持ち帰るか。実測6秒。**網が要る** —— 届かない時は
                                     # 2(未測定)を返す。緑に丸めない事
    test/fork-check-controls.sh      # `--fork-session` を測る台本が、**測れていない時に
                                     # 測れていないと言う**か。偽の claude を7通りに振る舞わせる
                                     # ので上限を1トークンも食わない。実測2秒。網も不要
    test/resume-cwd-controls.sh      # `--resume` の引き当てが起動 cwd に固定されているか
                                     # (HANDOFF §3-V)を測る台本の対照。偽の claude を9通りに
                                     # 振る舞わせるので上限を食わない。実測2秒。
                                     # ★姉家族の fork-check より1段むずかしい —— あちらは
                                     # 「全部通る前提で中身を比べる」が、こちらは**③が落ちる事**
                                     # を根拠に結論を書く。落ちた理由の取り違えが唯一の穴なので、
                                     # `anchor-broken`(同じ cwd でも引き当てない)と
                                     # `weird`(断り文句だけ違う)の2つが対照の本体。
                                     # どちらも③の観測だけ見ると正解筋と**見分けが付かない**
    test/interrupt-honesty-control.sh # ★2026-08-08。割り込みが「撃った事」を「止まった事」
                                     # として名乗らないか(§2.64)。木の外の写しを6通り壊して
                                     # 毎回赤が出る事を見る。実測5秒、本物の子は起動しない。
                                     # ★据えた初回に1件捕まえた: 「撃った所で死んだ事にする」
                                     # 変異が**緑のまま通った** —— 偽の期限が同期で発火した後に
                                     # 解決済みの約束へ `.then` を繋ぐと、競走は期限の側が勝つ。
                                     # 返り値ではなく**死の約束そのもの**を見る検査を足して塞いだ
    test/limit-lifted-controls.sh    # 「上限が明けたか」の門番が両方向に壊れていないか。
                                     # fake HOME なので edith も上限も要らない。実測1秒。
                                     # ★据えた初回に2件捕まえた: haiku を根拠にする誤答と、
                                     # `.js` が repo 内で crash する(= /tmp では動く)拡張子の罠
    test/post-gate-batch-controls.sh # 窓が開いた後の一発勝負を撃つ台本の**門**。偽 ssh/scp なので
                                     # edith も上限も要らない。実測1秒。★**開く方向**が本体 ——
                                     # 「閉じている時に閉じる」は本物で撃てるが、「明けたら撃つ」は
                                     # 窓が開くまで本物では測れない。片方向だけ見ると
                                     # 「常に閉じる門」を緑と読む
    tools/rc-backend-launch-check.sh # ★2026-08-03 に此処へ入れた。それまで**参照 0 件**
                                     # (3箇所在った参照は全部「検査は此処に在る」と書いた
                                     # 注記で、誰も回していなかった)= この runner が潰す為に
                                     # 在るまさにその病気。中身は偽の tailscale / node / dir で
                                     # 起動ラッパを実走する対照 38 本。実測2秒、本物の
                                     # `tailscale serve` は撃たない。`test/` ではなく `tools/`
                                     # に在るのは置き場所の歴史的な揺れで、性質は対照
    tools/serve-decision-check.sh    # ★2026-08-07 に此処へ入れた。上と**同じ病気の2件目**で、
                                     # 今度は 誰も気付いていなかった: 宣言(controls-for)は
                                     # 在るのに名前が `*-control*.sh` に当たらないので、
                                     # 「未登録の対照」を数える網が構造的に見られなかった。
                                     # = 一度も回っていない。実測 0 秒、全 19 ケース + 旧判定
                                     # との対比(動いた 14 行それぞれで旧が誤りだった事まで確認)
    tools/funnel-exposure-check-control.sh # ★2026-08-26 新設と同時に此処へ入れた(上2件が
                                     # 「後から登録して、それまで一度も回っていなかった」ので、
                                     # 同じ穴を3度目は開けない)。測る相手は
                                     # `funnel-exposure-check.sh` =「**公開されている入口の下に
                                     # 机へ向く経路が無いか**」。前身は「9443 が Funnel されて
                                     # いないか」で、Funnel が 443/8443/10000 しか扱えない以上
                                     # **原理的に真にならない空虚な条件**だった。対照は
                                     # 「赤に倒れる入力が2件以上ある事」自体を検査している
    tools/exposure-standing-control.sh # ★2026-08-26 新設と同時に登録。測る相手は
                                     # `health-observer.sh` の露出の節 =「**露出 / 判らない /
                                     # 安全**を取り違えないか」。露出は閾値なしで1回で鳴り、
                                     # 「測れない」は別の文言で緩い閾値、安全では黙る。
                                     # 偽 tailscale + 偽 通知先で駆動するので本物を触らない
    tools/desktop-attach-control.sh   # ★2026-08-26 新設と同時に登録。測る相手は
                                     # `desktop-attach.sh` =「机側の入口が **正しい機体の
                                     # 登録簿**を読むか」。Jervis にも同じ形の登録が 47 件
                                     # 在るので、手元を読むと**無関係な会話を rc=0 で返す**
                                     # (宛先だけ Friday の窓なので、出力はそれらしく見える)。
                                     # 偽 ssh で駆動するので、Friday が落ちている日に
                                     # 「対照が赤」= 実装の欠陥と読み違えない
    tools/mixed-index-control.sh      # ★2026-08-26 新設と同時に登録。測る相手は
                                     # `staged-controls-gate.sh` =「**緑が commit に載る版を
                                     # 測っているか**」。門は作業木を走らせ、commit は index を
                                     # 載せるので、`MM` の時 緑は載らない版を証明する。
                                     # 実際に起きた: add の後に直して add し直さず commit、
                                     # 門 12/12 緑・履歴は壊れた版。使い捨ての git repo を
                                     # 立てて測るので、本物の木を汚さない
    ios/tools/sim-device-control.sh   # ★2026-08-26 新設と同時に登録。測る相手は
                                     # `ios/tools/sim-device.sh` =「対照が **どの機へ
                                     # install しに行くか**」。此処が緩んでいた間、対照を
                                     # 回すたびに **Tom が見る機**が種なしの Debug 版へ
                                     # 差し替わっていた(緑にも healthz にも出ず、画を
                                     # 撮って初めて分かった)。偽 xcodebuild / 偽 xcrun に
                                     # 渡された引数で測るので、宛先の組み立て方が変わっても
                                     # 追随する(文字列 grep の写しの述語にしない)
    test/remote-mini-root-controls.sh # ★測る相手が **この repo の外**(`~/.claude/tools/
                                     # remote-mini.sh`)に在る唯一の対照。それでも此処に居るのは、
                                     # 回す物が此処しか無いから ——「誰も回さない対照は対照でない」。
                                     # 偽 ssh/rsync + 偽 HOME で、edith にも Tom の tmux にも
                                     # 触らない(偽 ssh は tmux/osascript/curl/wget を含む命令を
                                     # 実行拒否する = 手元で走る偽 ssh が本物の tmux へ
                                     # send-keys する事故を構造で塞ぐ)。実測25秒。
                                     # ★Claude Code の版が上がったら **これを回す** ——
                                     # `--resume` の探索鍵が slug だけ、という実測(
                                     # `.harness/evidence-2026-08-03/resume-scope-measurement.md`)
                                     # の上に写像設計が乗っているので、版が変われば前提が動く
    test/mutation-verdict-controls.sh # 門の外へ出した変異の判定(`tools/mutation-verdict.sh`)が
                                     # **今の木の物でない緑**を返さないか。木を丸ごと砂場へ写して
                                     # から触るので、repo にも走行中の変異にも触らない。
                                     # ★**この一覧で一番長い(実測 90〜150 秒)**。理由は
                                     # 「本物の走行を1件回して判定を作る」から —— 手で書いた log を
                                     # 食わせた対照は、log の形についての思い込みごと緑になる。
                                     # `RC_VERDICT_CTL_FAST=1` で §5-9 を飛ばせるが、その時は
                                     # **2(未測定)を返す**。速い方を緑と読ませない為
    test/mutation-timeout-controls.sh # 変異の脚が**時間切れ**になった時の処分。緑にも赤にも
                                     # 丸めず「未測定」で1件だけ落として走行は続ける、を測る。
                                     # 実測5秒(偽の脚 = `sleep`。検査一式は回さない)。
                                     # ★2026-08-04 まで die して走行ごと止めていた。M111 の様な
                                     # 「固まる事が欠陥の症状」な変異を1つも置けない形だった
    test/mutation-freeze-controls.sh # 変異の走行が**走っている最中の編集で汚れない**か。
                                     # 実測 150〜200 秒。走行を1本起こすので、別の走行が
                                     # 動いている間は **2(未測定)**を返す。
                                     # ★2026-08-03 に実際に汚れた事の再発防止 —— 3時間半の
                                     # 走行の途中で repo を10 file 触り、その1つが散文規則を
                                     # 破って `npm test` を赤にした結果、以降の変異が全部
                                     # 「検出」と記録された(= 素通りが丸ごと隠れ、要約は
                                     # 「素通り: なし」= **緑の方向に壊れた**)
    test/coldboot-chain-controls.sh  # 停電の後 edith が自力で戻れるかを測る道具
                                     # (`tools/coldboot-chain.sh`)。偽の fdesetup/
                                     # defaults/pmset + **本物の PlistBuddy に本物の
                                     # plist** を読ませるので、edith にも電源にも
                                     # 触らない。実測1秒未満。
                                     # ★元は deploy 9b の heredoc。切り出した理由は
                                     # 「注釈が名指しした 7 つの内 4 つしか読んでいな
                                     # かった」= heredoc には対照が書けないから
                                     # 誰も気付けなかった。旧版に差し替えると
                                     # **17 本全部が倒れる**(旧版は常に 0 で終わる)
    test/departure-survivability-controls.sh # ★2026-08-07。渡米前に3週間の無人耐久を
                                     # Jervis から1回で測る道具(`tools/departure-
                                     # survivability-check.sh`)。偽 ssh なので edith にも
                                     # tailnet にも触らない。実測1秒未満。
                                     # ★上の coldboot と**役割を分ける**: 冷起動の 7 項目は
                                     # あちらが持ち、此処は「その終了コードの引き取り方」
                                     # (0/1/2/欠落)と、鎖の外に在る 4 項目(tailnet の鍵 /
                                     # 常駐の現況 / 面の 401 / 空き)だけを見る。
                                     # ★据えた初回に1件捕まえた: `--days` を値無しで渡すと
                                     # `shift 2` が失敗して $# が減らず**無限ループ**した
    test/commit-suite-gate-controls.sh # commit の直前に単体の一式を回す門
                                     # (`tools/commit-suite-gate.sh`)。偽の一式を差すので
                                     # 本物の `npm test` は回さない。実測1秒未満。
                                     # ★本体が守るのは「rc は 0 なのに落ちた検査が在る」形と
                                     # 「集計行が出ていない(= そもそも走っていない)」形。
                                     # 素朴な rc だけ見る版に差し替えると 11 本中 8 本が倒れる
    test/staged-controls-gate-controls.sh # commit が**触れた対照**を回す門
                                     # (`tools/staged-controls-gate.sh`)。偽の repo の木 +
                                     # 偽の staged 一覧なので、本物の git にも repo にも
                                     # 触らない。実測1秒未満。
                                     # ★上の門(単体の一式)と役割が違う: `test/*-controls.sh`
                                     # は `npm test` の一部ではないので、上の門では対照の
                                     # 回し忘れが素通りする。**同日に2回踏んだ**方の穴。
                                     # ★一番危ない壊れ方は「選び方が空振りして、いつも
                                     # 『触れた対照は無い』と言う」—— 普段の commit では
                                     # 区別が付かないので S15 が陰性対照を張っている。
                                     # 道具だけ見る素朴版に差し替えると S6/S7/S8 が倒れる
    test/warn-ledger-controls.sh     # 「門ではない検査」の結果を捨てずに持ち帰る帳面
                                     # (`tools/warn-ledger.sh`)。source して使う関数なので
                                     # 偽の命令(`/bin/bash -c "exit N"`)だけで測れる。実測1秒未満。
                                     # ★守るのは**丸め方**: 255(ssh が繋がらない)を赤に混ぜると
                                     # 「edith が答えない」が「edith の停電対策が壊れている」として
                                     # 残り、居ない相手を直しに行く事になる。2(未測定)を緑に
                                     # 丸めるのも同じ族。
                                     # ★一番危ない壊れ方は「1 段も記録していないのに異常なし」——
                                     # 全部緑の配備では区別が付かないので W10/W11 が張っている。
                                     # 素朴な `|| true` 版に差し替えると 16 本中 14 本が倒れる
                                     # (W12/W13 は素朴版でも緑 = 見分けていない事を明記)
    test/copied-tree-controls.sh     # 単体スイートが**木の写しでも回る**か。写しで回る事は
                                     # 行儀ではなく機能要件 —— 変異台本は写しで `npm test` を
                                     # 回し、その赤/緑が「変異を検出した」の判定そのものだから。
                                     # 木の外を読む1行で対照1が死に、197 件が1件も回らなくなる
                                     # (2026-08-04 実測。手元は 580/580 緑のまま)。実測15秒。
    test/run-controls-controls.sh    # ★**この台本自身**の対照。砂場に自分の複製を建てて
                                     # 偽の子を並べるので、本物の対照は1本も走らない。実測1秒未満。
                                     # 自己言及に見えるが循環しない —— 走るのは砂場の複製の方。
                                     # ★据えた初回に1件捕まえた: 旧版は `exit $(( red > 0 ))` で、
                                     # 画面に「未測定(緑ではない)」と出しながら**終了コードは 0**を
                                     # 返していた。この lane では変異の走行中に 2 が出るのが常なので、
                                     # 一番よく起きる状況で緑になっていた。旧版に差し替えると
                                     # **R4 だけ**が倒れる(他 18 本は旧版も持っていた性質)
    test/port-coverage-controls.sh   # ★2026-08-05。C群移植の**分母**を測る道具の対照。
                                     # 道具は「JS の検査が食わせた入力が Swift の検査にも
                                     # 在るか」を見る。訂正6-1(`nextHistoryLimit(0)` の
                                     # 移し忘れ = `||` と `??` の割れ目)を人手で見つけた後、
                                     # **同じ物を機械で数える為**に据えた。実測1秒未満。
                                     # ★この道具の一番痛い壊れ方は偽の緑(移っていない入力を
                                     # 「在る」と言う)なので、対照の重心は語境界(P3)と
                                     # 判定しない種別(P5-P7)に置いてある。3種の変異で
                                     # 倒れる集合が重ならない事を実測済(台本の頭の表)。
    test/deploy-to-edith-controls.sh # 配備台本(本番の木を上書きする 600 行)の対照・第1弾。
                                     # ★型は**構造検査**(台本の文字列を読む)であって挙動検査
                                     # ではない。捕まえるのは「脚を足して除外を書き忘れた」
                                     # 「除外を消した」「正本に見える写しが増えた」の3つだけで、
                                     # **これが緑でも「配備は安全」とは言えない**。実測1秒未満。
                                     # ★据えた初回に1件捕まえた: `FOREIGN=(--exclude '.git/'
                                     # --exclude '.gitignore')` が**一度も展開されていなかった**。
                                     # 本当の保護は remote heredoc 内のリテラル4箇所で、配列は
                                     # 「正本に見える写し」。3つ目を足した人は全部直したと信じて
                                     # 何も変わらない。旧版(HEAD)に差し替えると **E4 だけ**が倒れる。
    test/deploy-to-edith-behavior-controls.sh
                                     # 同・第2弾 = **挙動検査**。砂場に台本を置き、PATH の先頭に
                                     # 偽 ssh / 偽 rsync を据えて**実際に走らせ**、呼び出しの log を
                                     # 読む。第1弾では届かない性質を測る:
                                     #   ★赤い検査(単体 / e2e / 起動ラッパ)の後、本番の木に
                                     #     触る呼び出しが **1 本も出ない**事。
                                     # これは台本を読んでも判らない(`set -e` の抜け / `|| true` の
                                     # 置き所 / 段の並び替えのどれか1つで壊れ、壊れた事は**本番で
                                     # 初めて判る**)。空振り防止に B0c = 「通れば触っている」を
                                     # 陽性側で釘付けにしてある。陰性 N1-N5 付き。実測4秒。
                                     # ★これでも言えない事: 偽 rsync は転送しないので、除外の旗が
                                     # 本当に `.git/` を守るか。→ 第3弾(次)が実測で塞いだ。
    test/rsync-exclude-controls.sh
                                     # 同・第3弾 = **実測**。台本から option 文字列を抜き、
                                     # **本物の rsync** を砂場で走らせて、宛先の `.git/` と
                                     # `.gitignore` が残る事を見る。第1弾は「旗が書いてある」、
                                     # 第2弾は「本番に触らない」までで、**旗が効く**は誰も
                                     # 測っていなかった(両弾が自分でそう書いていた)。
                                     # 守っている物 = edith 側の他人の `.git`(fleet の整備が
                                     # 2026-08-03 12:52 に置いた物。私の物ではない)。
                                     # 陰性 N1/N2 = 旗を外すと本当に消える(消えないなら
                                     # 上の緑は「守っている」でなく「元々消えない」を見ている)。
                                     # ★ここで測るのは**手元の** rsync。入れ替え/戻しは edith 側で
                                     # 走るので、向こうの実測は EDITH_CTLS の姉家族が持つ。実測1秒。
    ../ios/tools/ui-fixture-absence-control.sh # ★2026-08-05 に此処へ入れた。Sprint 2 の
                                     # `RC_UI_FIXTURE`(List 画面の UI テスト用 fixture 切替口、
                                     # `ios/Sources/Core/SessionsListingFixture.swift`)が
                                     # Release バイナリの文字列表に一切残っていない事の対照。
                                     # `#if DEBUG` を信じるだけでは確認にならない(条件コンパイルで
                                     # 落ちたはずの文字列が最適化で残る事がある)ので、実際に
                                     # Release/Debug 両方の iphonesimulator ビルドを起こし、
                                     # `strings | grep -c` で直接見る。Debug 側は錨(anchor):
                                     # 検索方法そのものが壊れて常に0を返す病気だと、Release の
                                     # 「漏れていない」0 と見分けが付かない。`grep -c` は0件の時
                                     # 終了コード1を返すので、この対照は `set -e` を使わず件数を
                                     # 変数へ受けてから比較する(brief 本文が名指しで警告した罠)。
                                     # ビルド自体の失敗は2(未測定)、漏れの検出は1(赤)と、
                                     # 「まだ測っていない」と「測って赤」を同じ籠に入れない。
                                     # 実測: xcodebuild を2構成分走らせるので数十秒〜数分掛かる。
    ../ios/tools/ui-fixture-behavior-control.sh # 同・第2弾 = **挙動検査**。第1弾(文字列走査)は
                                     # brief 本文が「脆い」と名指しした通り、バイナリに文字が
                                     # 無くても別経路で同じ状態に落ちるバグは拾えない。ここでは
                                     # Release の .app を headless simulator へ実際に install/launch
                                     # し(`SIMCTL_CHILD_RC_UI_FIXTURE=list-empty`)、Sprint 1 の
                                     # `KeyEntryViewModel.swift` の診断 print と同じ convention で
                                     # `RootView.swift` に足した `"root flow:normal"` /
                                     # `"root flow:fixture state:..."` を `simctl launch --console`
                                     # 越しに読む(鍵もホストも書かない、通った経路の名前だけ)。
                                     # 期待: "normal" は出る・"fixture" は一切出ない、の両方が
                                     # 揃って初めて緑 -- 片方だけでは「たまたま今回は退避した」を
                                     # 緑と誤読しかねない。`open -a Simulator` は使わない
                                     # (Tom の GUI を奪わない禁則、この repo 全体の方針)。
                                     # 実測: install + 起動待ち4秒 + ビルドで1分前後。
    test/verify-log-controls.sh      # `tools/verify-log.sh`(検査の出力と終了コードを残す包み)。
                                     # ★据えた理由は実害から: `loop-replan-gate.sh survival` が
                                     # `choice-reply` に赤を出したのに**出力を捨てる**ので、赤の
                                     # 理由を一言も言えなかった。仮説を3つ潰す羽目になり(ポート
                                     # 衝突・`npm test` 同時・機械の負荷、全部外れ)、機序は今も
                                     # 未確定。**根は「機序が分からない」ではなく「計器が証拠を
                                     # 捨てる」**。包みが判定を書き換えたら repo 全体の生死が嘘に
                                     # なるので、測る中心は「終了コードが素と一字一句同じか」。
                                     # 陰性 N1 = 握り潰す版は失敗を 0 で返す(この repo は
                                     # `mutation-verdict.sh assert` で同じ病気を既に踏んでいる)。
                                     # 実測1秒未満。
    ../ios/tools/sim-log-summary-control.sh # ★2026-08-05。ios/tools/build.sh --sim が印字する
                                     # **要約1行**の判定(ios/tools/sim-log-summary.sh)。
                                     # ★この2つを backtick で囲まない事。`test/no-linerefs.test.mjs` は
                                     # backtick の引用を実在検査に掛けるが、`test/copied-tree-controls.sh`
                                     # が作る写しには **ios/ が入っていない**(backend の変異走行用なので
                                     # 入れるのも違う)。囲むと「完全な木では緑・写しでだけ赤」になり、
                                     # commit の門は通って**変異走行の中でだけ**落ちる —— 検査 file 自身が
                                     # 頭で警告している罠に、2026-08-05 に此処で一度嵌まった。
                                     # 隣の ui-fixture 2本が ios/ の `.swift` を囲めているのは、
                                     # 引用検査の対象拡張子が mjs/sh/py だけで `.swift` が入っていないから
                                     # = **たまたま**であって、真似してよい形ではない。
                                     # 据えた理由も実害から: 旧版は `Executed N tests` 行を
                                     # `tail -1` で拾っていたので、bundle が2本(単体97 / UI3)在る
                                     # この repo では 100 件走った run が「Executed 3 tests」と出た。
                                     # Sprint 1/2 の Generator と Evaluator が**4者とも別々に**
                                     # log を grep し直して回避しており、誰も計器の方を直していない。
                                     # ★本当の害は数字違いではない: 単体 bundle が1件も起動しなくても
                                     # UI 3件が緑なら「失敗0件」と出る = **偽の緑の道**。DoD が読むのは
                                     # この1行なので、ここが嘘を吐ける事は sprint 全体の証拠が嘘になる事。
                                     # 直した版は `Test Case ... passed/failed` の行を数える(1件=1行、
                                     # bundle を跨いでも二重に数えない)。0件は緑ではなく2(未測定)。
                                     # ★この対照は**本番の xcodebuild を起こさない**。判定を build.sh から
                                     # 別ファイルへ出して log を引数で受ける形にしたので、作り物の log で
                                     # 全分岐を測れる。測るのに数分掛かる造りだと対照は書かれない
                                     # (上の ui-fixture 2本が実際に数十秒〜数分掛かっている)。実測1秒未満。
    ../ios/tools/conversation-ui-control.sh # ★2026-08-06。会話画面の UI 検査
                                     # (ios/UITests/ConversationUITests.swift の4本)の負の対照。
                                     # 据えた理由が「書いた検査が空振りだった」なので、他の対照より
                                     # 一段直接的: 初版の4本は fixture が readable な応答を返そうが
                                     # 返すまいと緑だった(BUSY と「まだ screen を観測していない」は
                                     # 画面上まったく同じ = どちらも guard の既定へ落ちる)。
                                     # 錨(ライブの行1本)を足して直したので、その錨が効いている事を
                                     # 変異で確かめる。M2 = fixture の readable の枝を殺す、が要。
                                     # 作業木の Swift を変異させるので、殺された回の取り残しは
                                     # 固定 path の目印で次の走行が拾う(dod-sprint-6 と**共有**。
                                     # 同じ ConversationViewModel.swift を変異させる2本なので、
                                     # 目印を分けると互いの取り残しを基準点として複製してしまう。
                                     # ズレは rc-backend/test/mutation-recovery-copy.test.mjs が毎 commit 測る)。
                                     # 実測 2026-08-06: DerivedData が温まっていて 3.6 分
                                     # (基準 37s + 変異 39/48/49s)。冷えていればその分伸びる。
    ../ios/tools/list-return-refresh-control.sh # ★2026-08-08。一覧の**取り直しの引き金**の
                                     # 負の対照(変異5本)。上の姉家族と同じ手だが的が違う:
                                     # 会話画面ではなく `ios/Sources/Screens/List/ListView.swift` と
                                     # `ios/Sources/Screens/Shared/ForegroundResume.swift`。
                                     # ★据えた理由が此処では一段強い。S8-5 で判ったのは、Sprint 4 から
                                     #   会話画面の前面復帰(N4)が**一度も発火していなかった**事 ——
                                     #   守っていた検査4本(単体2 + 変異 N8c/N8d)は全部が**規則**を
                                     #   測っていて、「iOS がその引数でその関数を呼ぶか」という前提を
                                     #   誰も測っていなかった。規則は最後まで正しく、偽は前提の方。
                                     #   だから此処の変異は M4/M5 で**配線**を撃つ(規則が復帰を返さない /
                                     #   `.onChange(of: scenePhase)` ごと消す)。
                                     # ★M3 が主役: DEBUG fixture の通し番号を `1` で凍らせると、
                                     #   「起動は1回」の検査は**緑のまま通る**(起動時の正解も 1 だから)。
                                     #   回数を数える検査は器が死んでも気付けない = 錨が要る、という
                                     #   学びをそのまま対照にした。probe はこの緑を明示的に assert する。
                                     # ★M1(`.task` を消す)は的が広く、一覧の UI 検査6本**全部**が赤くなる。
                                     #   狭められない —— `.task` は初回取得も兼ねているので、消せば画面が
                                     #   出ない。M1 が測れているのは「`.task` が取得の唯一の入口」まで。
                                     # 実測 2026-08-08: xcodebuild 6回で **約8分**(基準 + 変異5、
                                     # DerivedData が温まっている時)。**今の一覧で一番高い** ——
                                     # 下の dod-sprint-6-controls.sh の 248 秒の約2倍。
                                     # それでも外さないのは、上に書いた欠陥を捕まえられる対照が
                                     # 此処にしか無いから(規則を測る対照は5 sprint 緑を出し続けた)。
                                     # 冷えて伸びる時の作法は下と同じ = **外すのではなく温めてから回す**。
    ../ios/tools/inflight-sentence-control.sh # ★2026-08-08(S8-6)。割り込みと打鍵が
                                     # **飛んでいる間**、画面が何を言うかの負の対照(変異5本)。
                                     # 上の姉家族2本と違って的が単体検査なので、`-only-testing` で
                                     # 2 class(ConversationViewModelTests / ConversationViewTests)
                                     # だけを撃つ = **UI 検査を1本も起こさない**。同じ手で
                                     # 一番安い形になっている。
                                     # ★据えた理由: この節が触ったのは「無言だった窓に一文を置く」
                                     #   だけの小さな変更だが、S8-5 と同型の穴が在る ——
                                     #   `interruptInFlightNotice` が常に nil を返す実装でも、
                                     #   文言を組み立てる側(`interruptInFlightText(timeout:)` を
                                     #   直接呼ぶ検査)は一切気付かない。**窓の中で観測する検査**と
                                     #   **文言を作る検査**は別物で、前者が死んでも後者が緑を出す。
                                     # ★M2 と M4 が主役: 打鍵の「飛んでいる間」は「文(飛んでいる事)」と
                                     #   「印(どの鍵か)」の2つで出来ていて、片方を殺すと**もう片方の
                                     #   検査は緑のまま**。probe はその緑を明示的に assert する ——
                                     #   つまり「文は出るが全ボタンが一斉に回る」版も
                                     #   「1つだけ回るが何も言わない」版も、片側の検査だけなら通る事を、
                                     #   主張ではなく毎回の走行の実測として出す。
                                     # ★M3: 秒数の直書き。今の `writeTimeout` は 30 なので
                                     #   直書きしても**文面は基準と同一**になる = 定数が動いた日にだけ
                                     #   嘘になる形。動かした値(45)で掴めるかを測る。
                                     # 実測 2026-08-08: xcodebuild 6回(基準 + 変異5)で
                                     # **real 111.94 秒**(= 約1分52秒、DerivedData が温まっている時。
                                     # 2回走らせて2回とも PASS 6 / FAIL 0 / UNMEASURED 0)。
                                     # ios の対照3本で**一番安い** —— 上の
                                     # list-return-refresh-control.sh の約8分、下の
                                     # dod-sprint-6-controls.sh の 248 秒に対して。
                                     # UI 検査を起こさない事がそのまま値段に出ている。
    ../ios/tools/signout-notice-control.sh # ★2026-08-08(#37 / 監査 X2-6、#40 / 監査 X2-8)、
                                     # 2026-08-11 に (c) を追加(#64)。
                                     # 鍵入力画面の負の対照(変異20本)。守っている物は
                                     # **3つ**在り、同じ file 群の上に載っている:
                                     #   (a) 401 で戻された時の「二度目である事」の運搬
                                     #       (断りの文 + 前の URL)…… M1-M9、DESIGN.md 2.65
                                     #   (b) 接続を押した後の「確かめています」の2段
                                     #       …… M10-M12、DESIGN.md 2.68
                                     #   (c) 焼き込んだ種で初回起動が一覧に着く事
                                     #       …… M13-M20、DESIGN.md 2.85 / 2.87
                                     # ★1本に束ねてあるのは staged-controls-gate.sh が
                                     #   **path で対照を選ぶ**から。別 file に割ると
                                     #   KeyEntryViewModel.swift を1行触るだけで xcodebuild が
                                     #   2組走る —— 同じ木・同じ simulator を測るのに費用だけ
                                     #   倍になる。概念の綺麗さより測定の費用を取った。
                                     # ★据えた理由: 継ぎ目が4つ在って、**どの1つが切れても
                                     #   残り3つの検査は緑を出す**。断りを作る所 / 器に
                                     #   書く所 / 起動時に読む所 / body が描く所。
                                     #   単体で届くのは前3つまでで、最後の1つは
                                     #   単体からは永久に見えない —— S8-5 と同型。
                                     # ★(a) の主役は M8 と M9。
                                     #   M8 = body の `if let` を殺す、M9 = `RootView` が
                                     #   `notice:` を渡すのを止める。どちらも
                                     #   **単体は1本も落ちない**まま画面から断りが消える形で、
                                     #   probe はその緑を明示的に assert する。
                                     # ★M2 = 順序だけの欠陥(断りより先に Keychain を消す)。
                                     #   間で殺された電話は「鍵も断りも無い」= 直す前と同じ
                                     #   白紙に落ちる。他の全部の検査が緑のまま通るので、
                                     #   順序を名指しで見る検査を別に置いてある。
                                     # ★M11 / M12 は (b) の主役で、これも UI class を撃つ。
                                     #   M11 = 「確かめています」の節を body から消す、
                                     #   M12 = 確かめている間に欄を打ち替えられる様にする。
                                     #   `viewModel.isChecking` までは単体で読めるが、
                                     #   `.disabled` が現に効いているかは単体からは見えない。
                                     # ★M13-M17 が (c) で、M17 が主役。`RootView` を
                                     #   「鍵を持っていても鍵入力画面を出す」形へ変えると、
                                     #   **単体は1本も落ちないまま**画面から一覧が消える ——
                                     #   2026-08-11 に Tom が実機で踏んだ欠陥と同じ形。
                                     #   M14/M15/M16 は幸せな筋道の検査が**緑のまま**である事を
                                     #   毎回 assert する = 鍵の回転が塞がる版・打った鍵を
                                     #   上書きする版・途中で殺されて白紙に落ちる版が、
                                     #   幸せな筋道だけの検査では素通りする事の実演。
                                     # ★M20 は (c) の**握り潰し**側。保存が throw した種に
                                     #   「蒔いた」の記録を付ける形へ戻すと、一度きりの保存
                                     #   失敗で次の起動が鍵入力画面に落ちる —— #64 が直しに
                                     #   来た形そのものが、`try?` 1個で戻る。M16(順序)は
                                     #   正しいままなので、**順序の検査は緑のまま**通る。
                                     #   probe はその緑を毎回 assert する(DESIGN.md 2.87)。
                                     # ★M18/M19 は「種として認めない形」を潰す2本で、
                                     #   `Provisioning` の入口を撃つ。M18 = 前後を削って
                                     #   空・空白・未解決の `${...}` を素通しにする、
                                     #   M19 = scheme と host の門を外して平文 URL を通す。
                                     #   どちらも `AppState` の幸せな筋道は**緑のまま**で、
                                     #   電話は「もっともらしい嘘の宛先」に繋ぎに行く ——
                                     #   起きるのは刻印とは無関係な顔をした故障で、Tom は
                                     #   机から遠い所でそれを追う事になる。
                                     # ★基準は UI class を**2つとも**撃つ(KeyEntryUITests +
                                     #   FirstRunUITests)。錨のうち FirstRun 側の1本は
                                     #   後者にしか居らず、基準で緑を見ていない名前の赤を
                                     #   成果に数えない為。
                                     # ★錨の本数は control 側で**数えて**印字する。
                                     #   2026-08-11 まで報告文に `21本` と直書きしていて、
                                     #   錨を足した人が報告文を知らない = 数が黙って嘘に
                                     #   なる形だった(同じ日に `extract()` の class 一覧でも
                                     #   踏んだ、写しが2つ在る型)。
                                     # 実測 2026-08-11: xcodebuild 21回(基準 + 変異20、
                                     # うち6回は UI class 込み)で **real 734.0 秒**
                                     # (= 12分14秒、DerivedData が温まっている時。
                                     # PASS 21 / FAIL 0 / UNMEASURED 0)。
                                     # M18/M19/M20 は単体だけで赤くなるので UI 回数は
                                     # 増えない(19本→20本の値段は 33.5 秒)。
                                     # (2026-08-08 の (a)+(b) だけの時は 13回 / 522 秒)
                                     # ★ios の対照で**一番高い**。下の
                                     # list-return-refresh-control.sh の 429 秒を抜いた ——
                                     # (b) を足す前(388 秒)は2番目だったので、この行は
                                     # #40 で書き換えている。値段の出所は UI 検査6回で、
                                     # それが此の対照の存在理由そのものなので、安くする道は
                                     # 「画面に出る事を測らない」しか無い。
                                     # 冷えて伸びる時は外さず温めてから回す。
    ../ios/tools/bar-material-control.sh # ★2026-08-08(#35 / 監査 X-3)。会話画面の
                                     # 灰色の帯(`.bar`)が composer だけの物である事を見張る
                                     # 検査(`rc-backend/test/bar-is-composer-only.test.mjs`)の
                                     # 負の対照(変異4本)。撃つのは node の検査1本だけなので
                                     # **xcodebuild を1回も起こさない** = ios の対照で最も安い(実測2秒)。
                                     # ★据えた理由: 守る対象が「背景の材質」で、材質は画面にしか
                                     #   現れない。XCUITest は色も材質も読めないので見張りは
                                     #   バイトの走査しか有り得ず、走査は整形・改名・枝の追加で
                                     #   **静かに的を外す**。外れた事は緑で出る。
                                     # ★M3(anchor-truncated)が主役: 主張は「入っていない」= 否定なので、
                                     #   切り出しが痩せると中身を見ずに緑になる。切り出しの終端を
                                     #   壊した時、**主張は緑のまま**で錨だけが赤くなる事を、
                                     #   主張ではなく毎回の走行の実測として出す。
                                     # ★書き始めた時「赤くなる検査が無いので対照は空回りになる」と
                                     #   書いた。手で変異を植えたら当の検査が赤くなり、前提が偽だと
                                     #   判ったので撤回した(訂正の順序は DESIGN.md の 2.63 に在る)。
    ../ios/tools/live-send-check-control.sh # ★2026-08-06。`ios/tools/live-send-check.sh` の
                                     # **判定だけ**の真理値表(21 本)。本体は edith と実機の会話が
                                     # 要るので、判定を `--verdict` へ切り出して観測値を引数で受ける
                                     # 形にした(conversation-ui-control.sh と同じ手)。実測1秒未満。
                                     # ★据えた理由: 終了コードの正本(rc-backend/tools/exit-codes.mjs)を
                                     #   shell からは import 出来ない = 写しの側だけが黙ってずれる。
                                     #   3(上限で測れていない)を 1(赤)より弱く扱う順序が要。
                                     # ★7 節(出力の文言)は陰性対照で穴が見つかって足した:
                                     #   上限の足を `--(測っていない)` でなく `ok` と印字する細工でも
                                     #   終了コードは 3 のままで、1-6 節は全部緑だった。
    ../ios/tools/live-interrupt-check-control.sh # ★2026-08-06。姉家族(上)と同じ手で
                                     # `ios/tools/live-interrupt-check.sh` の判定2つを撃つ(29 本)。
                                     # 輪が尽きた時(`--busy-verdict`)と最後の4つの足(`--verdict`)。
                                     # 本体を1回走らせて通るのは **verified の枝だけ** —— 残りは
                                     # 一度も走った事が無いまま書かれていた。実測1秒未満。
                                     # ★`.harness/live-interrupt-wording-controls.sh` とは別物:
                                     #   向こうは**掴む文が2つの file で一致しているか**、此処は
                                     #   **掴んだ後にどう裁くか**。同じ台本の別の穴。
    ../ios/tools/account-ui-control.sh # ★2026-08-12。口座の表示と切替
                                     # (REQUIREMENTS §4-5 / §5-8)の負の対照(変異4本)。
                                     # 守るのは全部「画面が机と食い違う」形の欠陥で、
                                     # 食い違ったまま緑になる物ばかり:
                                     #   A1 遅い読み取りが新しい切替を上書きする
                                     #      (Codex 2026-08-12 が名指しした操作列)
                                     #   A3 切替の失敗の後に読み直さない
                                     #   A4 切替の失敗を撃ち直す(= 二段進めて一段失敗と報告)
                                     #   A5 `.idle` を空で描く -> `.task` が繋がらず
                                     #      実機でも口座が永久に出ない(単体は1本も落ちない)
                                     # ★復元の正本は **git**(`git checkout --`)。最初の版は
                                     #   trap で戻す形だったが、SIGKILL された走行が変異を
                                     #   追跡ファイルに残す事故を同日に起こした。走行の入口で
                                     #   「索引との差が無い」を要求するので、trap が走らなくても
                                     #   次の走行が UNMEASURED で止まる。
    ../ios/tools/mutation-deferral-control.sh # ★2026-08-12(#66)。
                                     # `ios/tools/signout-notice-control.sh` の**選び方**の対照(17 本、
                                     # 陰性対照 4 本)。実測 **3.8 秒** —— `--which`(選択だけ
                                     # 答えて xcodebuild を1回も撃たない口)を使うので、
                                     # ios の対照で桁違いに安い。
                                     # ★据えた理由: 変異を面で絞る様にした結果、壊れ方が
                                     #   **二方向**になり格が違う。狭め過ぎ = 回すべき変異を
                                     #   落とす = 「全部緑」が**分母の痩せた緑**になり、
                                     #   欠陥を出荷しても誰も赤を出さない。狭め足りず = 遅いだけ。
                                     #   だから的は全部「落とし過ぎていない事」に向けてある
                                     #   (N1-N9 が全部回す側、N10-N13 が絞りが効いている側)。
                                     # ★2026-08-12 の実測: `Provisioning.swift` 1文字で
                                     #   変異 20 -> 2。落とした 18 本は名前で印字する。
    ../ios/tools/build-device-pick-control.sh # ★2026-08-09。`ios/tools/build.sh` が
                                     # **どの電話へ入れるか決める所**の対照(10 本)。
                                     # 据えた理由 = 此処が同日、実際に**嘘の赤**を出した:
                                     # 一覧の `tunnelState=disconnected` を門にしていたので
                                     # 繋がる実機を「no connected device」と断り、電話の側を
                                     # 疑いに行った。直後に同じ機へ install が通り、devicectl 自身が
                                     # `Acquired tunnel connection to device.` と言った ——
                                     # tunnelState は「いま管が張られているか」であって
                                     # 「張れるか」ではない。正しい値を持たない診断を判定にすると、
                                     # 嘘の赤で検査ごと信用されなくなる。
                                     # 入力は**本物の devicectl の出力**から個体を指す値だけ
                                     # 差し替えた物(tools/fixtures/devicectl-list-devices.json)。
                                     # 継ぎ目 = `RC_BUILD_SH`。変異5通りが狙った行だけを倒す事まで
                                     # 見た(M1 旧版へ戻す→A、M2 黙って先頭→C、M3 名指し外し→D、
                                     # M4 形の変化を空へ丸め→F、M5 xcrun の失敗を飲む→H)。実測1秒未満。
                                     # ★1本(I)だけは対象ではなく**入力**を測る = fixture に本物の
                                     # 個体が残っていない事。作り直す人は必ず実機の前に居るので、
                                     # 差し替え漏れがそのまま commit へ乗る(実際、欄の名前で潰したら
                                     # potentialHostnames に3通りの形で残っていた)。型で見張る。
    ../.harness/dod-sprint-3-controls.sh # ★2026-08-05。Sprint 3 の DoD 照合表
                                     # (`.harness/dod-sprint-3.sh`)の対照 = 15 行のうち 12 行が
                                     # **本当に赤にも緑にもなる**事を測る。照合表は「全部緑」を
                                     # 出した瞬間から誰も疑わなくなるので、逆向き(測っていないのに
                                     # 緑)を見張る物が要る。実際に初回で私の regex の基準点の誤りを
                                     # 3件捕まえた。ios/ を scratch へ写して**複製だけ**を壊す
                                     # (主作業木では Generator が xcodebuild を回している)。実測2秒。
                                     # ★sprint が閉じたら EXCLUDED_CTLS へ理由付きで移す事。
                                     #   消すのではなく移す —— 消すと「対照が在った」記憶だけが残る。
                                     # ★★2026-08-05、Sprint 3 は閉じた。だが**移さない**。理由:
                                     #   Sprint 4 で ios/ は大きく動いた(検査 150→212、Sources に
                                     #   5 file 追加)のに、この対照は 12/12 緑のまま通っている。
                                     #   churn を一つ跨いで緑な対照は「負債」ではなく**現に効いている
                                     #   計器**で、外せば守りがその分縮む。上の指示が想定していたのは
                                     #   「歴史的な DoD が腐って全員を止める」場面 —— それは起きていない。
                                     #   移すのは**実際に赤くなり、その赤が正当な進化だと確認できた時**。
                                     #   その日まで、指示に従わない判断をここに置く(黙って無視しない)。

    ../.harness/dod-sprint-4-controls.sh # ★2026-08-05。Sprint 4 の DoD 照合表の対照(27 本)。
                                     # 各行の緑・赤を**写しの中で自分で作る**。現在の判定を前提に
                                     # しないのは、Generator が同じ木の ios/Tests と progress.md を
                                     # 書き換えている最中にも走るから —— 「今 8-b が赤だから赤を
                                     # 期待する」と書いた対照は、直された瞬間に嘘になる。
                                     # 基準の一式 log すら写しの実数から合成する。
                                     # ★初回で本体の欠陥を1つ捕まえた: 失敗数を `$NF` で読んでいて、
                                     #   抜き出した文字列の最終語は "failures" という**語**なので
                                     #   常に 0 = 失敗を含む log を「失敗 0」と報告していた。
                                     # ★4 行目だけ3方向から撃つ(本文の Date() は赤 / **注釈の中の
                                     #   Date() は緑** / 時刻を受ける宣言が消えたら赤)。真ん中が要るのは、
                                     #   本体の初版が実装ではなく doc 注釈に当たって偽の赤を出したから。
                                     # 実測 12 秒。

    # ── ★2026-08-05、6本まとめて此処へ入れた(**登録漏れの実例**)──────────────
    # 経緯: この6本は同じ晩に書いて commit した。commit の門(`staged-controls-gate`)は
    # **その commit が触れた**対照を staged 一覧から導いて回すので、7/7 緑が出た。
    # 私はそれを「対照が生きている」と読んだ。読み違えである ——
    # **「触った時に回る」と「常に回る」は別の問い**で、常設の一式(此処)から見れば
    # 6本とも `UNREG = 一度も回らない対照` だった。実測 2026-08-05: disk 45 本 /
    # 未登録 6 本、全部この晩に私が書いた物。
    # 気付いたのは自分ではなく、並走していた Sprint 4 の Generator が進捗にそう書いたから。
    # 上の UNREG 照合は正しく赤を出していて、**私がそれを見ていなかった**だけである。
    # ★教訓は「一覧へ足せ」ではない: **緑を出した instrument が何を測っているのかを、
    #   緑を受け取る前に言える事**。commit の門の緑は「触れた物は緑」であって
    #   「守りが立っている」ではない。
    # 6本とも速い(実測: 13s / 1s / 1s / 0s / 1s / 0s = 合計 16 秒)ので既定へ入れる。
    test/doc-linerefs-gate-controls.sh # 書類の行番号引用ラチェットの門。緑/赤/**測れていない**の
                                     # 撃ち分け6本。偽 repo を `git init` して撃つので本物の
                                     # 基準値(393件)と 36 本の .md に依存しない。実測1秒。
    test/pre-commit-gates-controls.sh # commit の門の**呼び出し側**。どの門が呼ばれるか、
                                     # 絞り込みが何を通すかを、偽 repo とスタブで測る12本。
    test/install-hooks-controls.sh   # `.git/hooks/pre-commit` を据える台本。据え直しても
                                     # 壊れない事と、既存の hook を黙って上書きしない事。
    test/cited-testnames-gate-controls.sh # 受け入れ表・証跡が**実在しない検査名**を引いていないか
                                     # 見る門(7本)。要の1本は「表を1行も触らない commit
                                     # (= 検査を改名しただけ)で赤が出るか」。腐りを作るのは
                                     # 改名した側なので、表側だけを引き金にすると素通しする。
    test/vacuous-gate-controls.sh    # 「錨の無い検査」を書いた瞬間に止める門(11本)。
    test/vacuous-scan-controls.sh    # 同・走査の本体(10本)。
    test/port-coverage-gate-controls.sh # 移植の照合を**走らせる**門(10本)。道具側の対照は
                                     # 上の port-coverage-controls.sh。★対照が在る事は道具が
                                     # 回っている事を意味しない、が此処を足した理由(実測 08-07)。
    test/test-discovery-controls.sh  # 新しい test file が単体スイートに**拾われる**か。
                                     # 拾われない検査は書いた本人にだけ緑に見える。

    # ── ★2026-08-06、8本まとめて此処へ入れた(**登録漏れ、2度目**)─────────────
    # 前回(上の段落)は 2026-08-05 21:41 の commit `a3d6b58` で6本を登録した。
    # **この file はその時刻から今日まで一度も触られていない**。その間に書いた対照は8本、
    # 全部 08-06 の一日のうちで、**8本とも未登録**だった:
    #   00:41 live-interrupt-wording / dod-sprint-6   01:02 live-shell-key
    #   01:13 poll-spend   01:21 restart-epoch   16:26 verify-state-judgment
    #   17:27 phone-window-judgment   20:47 dod-sprint-6-recovery
    # 実測 2026-08-06: disk 上 **57 本** / 未登録 8 本。
    # (同日の progress.md に「56 本」と書いたが、それは `ios/tools/conversation-ui-control.sh`
    #  を昇格させる前の値で、書く時点で測り直していなかった。正は 57)
    # ★引用は木の名前から書く事。`ios/` にしか無い名前を此処で**裸**で引くと、完全な木では
    #   隣の木で解決して緑・`rc-backend/` だけの写しの中でだけ赤になる(2026-08-06 に此処で
    #   2回踏んだ)。写しは変異走行の判定器なので、そこでの赤は以降の変異を全部「検出」に
    #   化けさせる。今は `test/no-linerefs.test.mjs` の②-b が完全な木の側で此れを捕まえる。
    # ★前回の教訓(「緑を出した instrument が何を測っているのかを言える事」)は**言える様に
    #   なる**事しか要求しておらず、次に書く対照が登録されるかは私の記憶に懸かったままだった。
    #   記憶に懸けた守りは、1日で同じ形に戻る。此処に足すのは今日の8本の**始末**であって、
    #   再発の防止ではない。防止は門の側 = `tools/controls-registration-gate.sh`
    #   (対照を新設した commit が此の file を触っていなければ止める。同じ commit で足した)。
    test/controls-registration-gate-controls.sh
                                     # ★その門自身の対照(9本、陰性対照つき)。偽の repo を
                                     # `git init` して門を撃ち、新設だけ=赤 / 一覧も触った=緑 /
                                     # 変更だけ=素通し / 走査 dir の外でも赤 / 改名でも赤 /
                                     # 対照が無い commit は**無言**の緑 / 降ろす口は理由を残す /
                                     # git でなければ exit 2 を測る。8番目が陰性対照 = 門の
                                     # `exit 1` を潰した写しでは「新設だけ」が緑に変わる事の確認。
                                     # ssh も xcodebuild も要らず 3 秒。**この行自体が門の初仕事**
                                     # (門は自分の対照を新設した commit にも同じ要求をする)。
    test/phone-window-judgment-controls.sh
                                     # `tools/verify-phone-window.sh` の**判定**の対照。
                                     # 鎖③④の証拠 JSON を作る側なので、観測できなかった物を
                                     # 緑にすると本番判断が丸ごと嘘になる。観測(ssh / tmux /
                                     # launchctl / 8787)は測らない —— 本物のバイトを切り出して
                                     # mktemp の砂場で判定だけを撃つ。ssh は1回も呼ばない。
    test/poll-spend-controls.sh      # e2e の待ち道具(`pollUntilScreen`)が「回数を使い切った」
                                     # を赤の本文に出すか。言わないと赤が3通りに読め、次に直す人が
                                     # 「眠りを足す」band-aid に引き寄せられる。砂場の probe で撃つ。
    test/restart-epoch-controls.sh   # 再起動を跨いだ栞が**偶然一致しない**事。epoch が連番だと
                                     # 再起動後に古い栞が通り、電話は「最新です」と表示したまま
                                     # 永久に凍る —— 赤くなる場所がどこにも無い、此の系で最悪の
                                     # 壊れ方。SSE 側に実在した穴なので、対照が要る側の典型。
    test/verify-state-judgment-controls.sh
                                     # `tools/verify-rc-backend-state.sh` の**判定**の対照。
                                     # safety-core HARD GATE 1 が食う artifact を作る道具なので、
                                     # 此処が主観に落ちると「止まっている」「動いている」の
                                     # 判断が全部まとめて嘘になる。プローブ側は駆動しない
                                     # (本物の launchd と 8787 に触るので edith の上でしか動かない)。
    ../.harness/live-interrupt-wording-controls.sh
                                     # 実機の割り込み検査が掴む**文言**が、`view.mjs` と
                                     # 実機側で片側だけ変わっていないか。当たらないプローブが
                                     # 「無い」と報告する形を塞ぐ。grep だけで走るので瞬時。
    ../.harness/live-shell-key-controls.sh
                                     # 実機の殻(`ios/tools/*-main.swift` と呼び側の `.sh`)が
                                     # **鍵を漏らさない**事。argv にも環境変数にも置かない /
                                     # 印字しない / 既定のホストを書かない / 鍵に触る行が許した
                                     # 4形以外に無い。全部「書かれていない事」で守られている規律
                                     # なので、書かれていない事を測る物が要る。
    ../.harness/dod-sprint-6-recovery-controls.sh
                                     # `.harness/dod-sprint-6-controls.sh` の**復旧区間だけ**を的の本文から
                                     # 切り出して回す(写しは作らない)。xcodebuild も Swift も
                                     # 要らないので数秒。SIGKILL された回の変異が作業木に残る事を
                                     # 実測して据えた対照で、その復旧手順は今 ios の変異対照2本の
                                     # 共有物になっている(ズレは test/mutation-recovery-copy.test.mjs)。
    ../.harness/dod-sprint-6-controls.sh
                                     # Sprint 6 の負の対照(13 変異 x xcodebuild)。**一番高い**が
                                     # 除外しない: 費用は実測 248 秒(2026-08-06、commit の門の中。
                                     # DerivedData が温まっている時)で、既に一覧に居る
                                     # `ios/tools/conversation-ui-control.sh` の 156 秒と同じ桁。
                                     # 冷えていれば数倍に伸びる —— その時は**外すのではなく
                                     # 温めてから回す**(`ios/tools/build.sh --sim` を先に1回)。
                                     # ★`test/verdict-mutants.sh`(下の「わざと入れていない」)との
                                     #   違いは桁: 向こうは既定で exit 2 を返し、全部回すと 20 分超。
    ../.harness/dod-sprint-6.5-controls.sh
                                     # ★2026-08-07。Sprint 6.5(受入)の照合表
                                     # `.harness/dod-sprint-6.5.sh` の負の対照(13 変異)。
                                     # 上の 6 の対照(248 秒)と違って **xcodebuild を回さない** ——
                                     # 照合表が読む file だけを scratch へ複製して壊すので実測 5 秒。
                                     # 作業木は触らず、走行の前後で `git status` の差 0 行を観測して
                                     # 確かめている。
                                     # ★**build と同時に回さない**(2026-08-07 に踏んだ): `DOD_FULL=1` の
                                     #   照合と並走させると、複製した log と原稿の指紋が書き込み途中の物に
                                     #   なり、OK 13 の中身のまま終了コードだけ 1 で出る。単独で回す事。
    ../.harness/build-fingerprint-controls.sh
                                     # ★2026-08-07。↑の照合表の **0 行目が読む指紋**を作る側
                                     # (`ios/tools/build.sh --sim`)の対照。commit の門が
                                     # 「注記 — 対照を導けない道具: build.sh」と名指ししていた穴。
                                     # 受入の緑 6 行(0 / 1-b / 2-b / 3 / 5-a / 6-b)がこの指紋に
                                     # 懸かっているのに、指紋を**作る**側は誰も見ていなかった。
                                     # 測るのは順序(xcodebuild より前か)と範囲(Sources/Tests/
                                     # UITests のどれを変えても動くか)と、中身か mtime か。
                                     # 指紋の式は build.sh から切り出して回す = 写しを持たない。
                                     # xcodebuild は回さないので実測 1 秒未満。
    ../.harness/fixture-label-parity-controls.sh
                                     # ★2026-08-08。電話の fixture に手で書いた札が、
                                     # `rc-backend/src/view.mjs` が実際に出す文字列と一致しているか。
                                     # 一覧(`SessionsListingFixture.swift` / `routeLabel`)は 5 行中
                                     # 2 行(tmux / worker)がズレており、worker 行は fixture の方が
                                     # **綺麗**だった(`ワーカー・実行中` / 本番は `ワーカー・busy`)。
                                     # 選択カード(`PollFixture.swift` / `choiceView`)も同日に同じ形で、
                                     # escape が `中止(Escape)` —— rc-backend に存在しない文字列で、
                                     # Sprint 7 の割り込み注意文はその実在しないボタンを指していた。
                                     # = 画面を何度見ても欠陥が出て来ない形(見た目で騙す)。
                                     # 木を跨ぐので `.harness` に置いた(`rc-backend/test/` の宣言は
                                     # rc-backend からの相対に解決されて `ios/…` を名指せない)。
                                     # xcodebuild は回さず node だけ = 実測 2 秒。
                                     # ★飛ばしを緑と読まない事を対照自身が測っている(部分木の
                                     #   写しでは此の検査は正しく飛ぶので、そこが抜け道になる)。
    ../.harness/timeout-agreement-controls.sh
                                     # ★2026-08-08。↑の4面(S8-19/20/21/22)と**根が同じで媒体が違う**:
                                     # 電話は境界の向こうの値を写して持ち、写しを照合する者が居ない。
                                     # 文字列の次は秒数 —— サーバの `POLL_MAX_WAIT_MS`(20_000)は
                                     # `BackendSession.serverPollMaxWait = 20` として**手で写されて**おり、
                                     # 其の注釈は「離れない」と書いていたのに確かめる機械は無かった。
                                     # サーバが上限を伸ばした日、電話は先に諦めるので**正常な
                                     # 「何も起きていない 200」が網の障害として画面に出る**。
                                     # 加えて「最大30秒待ちます」が説明している client の対応も注釈だけで、
                                     # `SendClient` の1行を短い定数に替えると文は 30 秒と言い続けるのに
                                     # 要求は 8 秒で諦める —— 両側の単体検査は**両方緑のまま**通る。
                                     # 木を跨ぐので `.harness` に置いた(理由は1つ上と同じ)。
                                     # xcodebuild は回さず node だけ = 実測 4 秒。
                                     # ★背骨は陰性対照Ⓐ: 定数と文と画面検査を**揃えて** 30→45 に
                                     #   動かした木は緑でなければならない。此処が赤い検査は秒数を
                                     #   今日の姿に固定し、正しく直す人を「検査が通らない」で止める。
    ../.harness/wire-key-agreement-controls.sh
                                     # ★2026-08-08。↑と根が同じで媒体が3つ目 —— **鍵名**。
                                     # 電話の `Decodable` は**わざと緩く**復号する(欠けた鍵は nil /
                                     # [] / .unrecognized)。理由は正しいが、緩さが安全なのは
                                     # 「ズレに気付く者が別に居る」時だけで、其の者が居なかった。
                                     # 鍵名が片側で変われば復号は通り、例外は出ず、両側の単体検査は
                                     # 全部緑のまま **画面だけが痩せる** = 一番静かな壊れ方。
                                     # `display.choice` が改名されれば選択カードは二度と出ず、
                                     # `digest` が改名されればカードは出るのに何も押せない。
                                     # 木を跨ぐので `.harness` に置いた(理由は1つ上と同じ)。
                                     # xcodebuild は回さず node だけ = 実測 4 秒。
                                     # ★背骨は陰性対照Ⓐ: 両側**揃えて** headline→title に改名した木は
                                     #   緑でなければならない。縛るのは今日の綴りではなく一致の方。
                                     # ★走査から隠れる書き方(`extension X: Decodable`)を別に見張る:
                                     #   型宣言の行に何も残らないので、行を読む走査からは完全に消える。
    ../.harness/wire-vocabulary-agreement-controls.sh
                                     # ★2026-08-09。↑と根が同じで媒体が4つ目 —— **語彙の値**。
                                     # 誤り応答の4本も「封筒を builder へ切り出せば鍵名で測れる」
                                     # と思って測ったら**効かなかった**: 電話が読む鍵は
                                     # `display` / `code` の2つで、前者は e2e 13-D が、後者は
                                     # `recovery-codes.test.mjs` が既に守っている。測れて
                                     # いなかったのは鍵名ではなく**両側が焼いている文字列の値**。
                                     # 木を跨ぐので `.harness` に置いた(理由は1つ上と同じ)。
                                     # xcodebuild は回さず node だけ = 実測 3 秒。
                                     # ★背骨は陰性対照Ⓐ/Ⓑ: 両側**揃えて** `SENDABLE`→`READY_TO_SEND`、
                                     #   `SESSION_NOT_FOUND`→`SESSION_GONE` と改名した木は緑。
                                     #   値を縛って追認機にならない事は此処でしか主張できない。
                                     # ★`probe` が**倒れた項の名前まで**見るのが姉家族との違い。
                                     #   検査が6本在り1つの変異が複数を倒しうるので、
                                     #   「何かが赤くなった」では走査を殺す変異が
                                     #   「値の組を測れている証拠」として数えられる。
                                     # ★据えた初回に1件捕まえた —— ただし**検査ではなく対照の**
                                     #   前提の誤り(`SessionsListingFixture.swift` を消せば
                                     #   `RC_UI_FIXTURE` が消えると踏んだが、同じ綴りは他に3 file 在る)。
    test/fleet-account-cswap-controls.sh
                                     # ★2026-08-26。cswap を fleet-account 形式へ翻訳する層。
                                     #   偽 cswap + 録った実出力(test/fixtures-cswap-list.json)で
                                     #   回るので、この機体でも Friday でも同じ答えになる。
                                     #   出口は**本物の parser**(src/account.mjs)で受ける。
    test/tunnel-observer-controls.sh
                                     # ★2026-08-26。外からトンネルを叩く観測者。偽の HTTP を
                                     #   127.0.0.1 に立てるだけで本物の tailnet に触らない。
    test/phone-window-health-controls.sh
                                     # ★2026-08-26。窓は在るのに中が固まっている状態を捕まえる。
                                     #   本物の tmux に別名の session を立てて回す(本番の work には触らない)。
    test/phone-window-notify-controls.sh
                                     # ★2026-08-26。↑の赤を Tom へ届ける段。偽の health と偽の通知先で
                                     #   回すので、鳴らし過ぎと鳴らな過ぎの両方を陰性で押さえている。
    test/deploy-to-friday-controls.sh
                                     # ★2026-08-26。Friday 向けの殻が渡す env が本当に
                                     #   exec へ届くかを、字面ではなく env で実測する。
                                     #   ssh も rsync も撃たない(exec を env に差し替える)。
                                     #   17 本のうち 9 本が「緑しか出せない計器」を潰す陰性。
)
# ── ★ここに**わざと入れていない**物(消えた訳ではない)────────────────────────
# `test/verdict-mutants.sh` = `test/mutation-verdict-controls.sh` の陰性対照
#   (対照そのものを壊して、狙った項が本当に赤くなるかを見る)。LOCAL_CTLS には**入れない**。
#   理由: この道具は既定で終了コード **2(未測定)** を正直に返す。遅い 6 体は親の対照が
#   本物の変異の走行を2回起こす為に 1 体 150〜250 秒 = 全部で 20 分超あり、pre-commit の
#   門の中では回せないから。ここへ登録すると commit が**毎回**止まる。
#   **止まる門は外される** —— 常に止まる門は「今回は飛ばす」を習慣にし、最後には門ごと
#   捨てられる。門に置かない方が、門が生き残る。
#   回し方: `RC_VERDICT_MUTANTS_SLOW=1 bash test/verdict-mutants.sh`(全 8 体、20 分超)。
#           既定(2 体だけ)は `bash test/verdict-mutants.sh` = exit 2。
#   ★「登録漏れ」に見えても直さない事。直すなら先にこの段落を読んで、上の理由が
#     まだ成り立つか(= 遅い 6 体が速くなったか)を確かめる。
# ── ★意図して一覧に入れない物(理由付きで**此処に**置く)────────────────────────
# 空でよい。空である事に意味が在る —— 下の照合は「此処に無い = 登録漏れ」と読むので、
# 外す判断をした時は必ず1行足す事。外した理由が書いていない除外は、次に読む人には
# 「漏れを黙らせた跡」と区別が付かない。
# (`test/verdict-mutants.sh` は名前が `*-controls.sh` ではないので照合の網に入らない。
#  外す理由は上の段落に書いてある)
EXCLUDED_CTLS=()

EDITH_CTLS=(
    test/env-death-controls.sh
    test/phone-window-controls.sh
    test/rsync-exclude-edith-controls.sh
                                     # 第3弾の edith 側。同じ台本を `RC_RSYNC_EXCL_WHERE=edith`
                                     # で回すだけ(写しを2つ持たない)。入れ替えと戻しの rsync は
                                     # remote heredoc の中 = **向こうの binary** が走るので、
                                     # 手元の緑は向こうの保証にならない。砂場は向こうの mktemp、
                                     # 小片が自分で消して `LEFT=0` を報告する(恒久物を置かない)。
)

list=("${LOCAL_CTLS[@]}")
[ "$ALL" -eq 1 ] && list+=("${EDITH_CTLS[@]}")

green=0; red=0; unmeasured=0
declare -a red_names=() unm_names=()

# ── ★一覧に載っていない対照を**赤**にする(2026-08-04、実際に漏れた)──────────
# 上の2つの一覧は**手で書いている**。`prove-all-controls.sh` は継ぎ目を毎回探すので
# 足し忘れが構造的に起きないが、**この台本は起きる** —— 冒頭に「足し忘れが起きない
# 造りにした」と書いてあるのは prove-all の話であって、此処の話ではなかった。
# 実測(2026-08-04): 対照が 30 本在るのに登録は 29 本。漏れていた 1 本は、同じ日に
# 書いた `copied-tree-controls.sh`(= 197 件の変異が回らない事を 15 秒で名指しする物)。
# **書いた対照が一度も回らない**のは DESIGN (19) そのもので、しかも書いた本人には
# 「対照を足した」という記憶だけが残るので、一番気付けない形で守りが1本消える。
#
# 未測定(2)ではなく**赤(1)**にする。回せなかったのではなく、回す物が無い事が
# 確定しているから —— 「測れなかった」と「守りが欠けている」を同じ籠に入れない。
# ★★2026-08-05: 見る範囲を **3つの木**へ広げる(門と同じ集合)。
#   此処は `test/*-controls.sh` しか見ていなかったので、`ios/tools/` と `.harness/` の
#   対照は**登録を忘れても誰も言わない**状態だった。実測: disk 上 38 本に対し、
#   どの一覧にも無い物が 1 本(`.harness/dod-sprint-3-controls.sh` = 今夜書いた物)。
#   ios 側が今まで無事だったのは手で入れていたからで、instrument が見ていたからではない。
#   ★これで「守りの届く範囲が欠陥と一緒に縮む」形の**5箇所目**。門(SCAN_SPECS)側は
#     一覧を1本に畳んだが、此処は走らせる順番と ALL の分岐を持つので配列は残す ——
#     代わりに**照合の網だけ**を門と同じ導出(走査 dir 全部)に合わせる。
#   単数形も拾う(`*-control*.sh`)。rc-backend 側の集合は変わらない(34 → 34、実測)。
#
# ★★★2026-08-05(同日・後): 上の「合わせる」が**写しだった**。走査 dir 3つを此処に
#   手で書いてあったので、門の SCAN_SPECS に4つ目が入った日に、此処だけが黙って
#   3つのまま残る。門側は同じ問題を「一覧を1つにして残りを導出」で根治したのに、
#   その導出の**外側**に写しが1枚残っていた —— 6箇所目、そして初めて
#   「踏む前に数えて見つけた」1件(他の5件は全部、赤か事故が先に来た)。
#   同じ一覧が2箇所に居る限り、要素を足す作業が片方を置き去りにするのは
#   注意力の問題ではなく**既定の結果**。だから網は門から取り出す。
#   取り出せなければ空にせず**未測定(2)で止める** —— 網が空だと全部が「登録済み」に
#   見えるので、黙って 0 本を走査するのがこの道具の一番危ない壊れ方。
#
# ★★★★2026-08-07: 上の「門から取り出す」が**半分だけ**だった。取り出していたのは
#   SCAN_SPECS の**1列目(走査 dir)だけ**で、3列目の拾い方(`name` / `decl`)は捨てて
#   此処で `*-control*.sh` の名前 glob を当て直していた。写しは dir の一覧ではなく
#   **述語**の側に残っていた、という事。門は同じ名前 glob を 2026-08-05 に測って
#   捨てている(門の `_scan_ctls` の直前の注釈)のに、此処だけが拾い直していた。
#   実測 2026-08-07(disk の現物に両方の網を当てた):
#     偽陽性 4件  tools/{run-controls,staged-controls-gate,prove-control,prove-all-controls}.sh
#                 = 宣言が無いのに名前が当たる。**門が自分自身を「未登録の対照」と呼ぶ**
#     偽陰性 2件  tools/rc-backend-launch-check.sh / tools/serve-decision-check.sh
#                 = 宣言は在るが名前が当たらない
#   ★後者の `serve-decision-check.sh` は**どの一覧にも無く一度も回っていなかった**。
#     「一度も回らない対照」を数える為の検査が、その1本を構造的に隠していた。
#   ★直し方として「此処でも3列目を読む」を先に捨てた —— 述語の写しが2枚になるだけで、
#     6回続いた再発と同じ手。**持っている側に聞く**(門の `--all-controls`)。
GATE_FOR_SPECS="${GATE_FOR_SPECS:-$ROOT/tools/staged-controls-gate.sh}"
REPO_ROOT_FOR_GATE="$(cd "$ROOT/.." && pwd)"
SCAN_CTLS=()
while IFS= read -r _p; do
    [ -n "$_p" ] || continue
    # 門の答えは repo の根から。此処の cwd は rc-backend なので基点を移す。
    case "$_p" in
        rc-backend/*) SCAN_CTLS+=("${_p#rc-backend/}") ;;
        *)            SCAN_CTLS+=("../$_p") ;;
    esac
done < <(RC_GATE_ROOT="$REPO_ROOT_FOR_GATE" /bin/bash "$GATE_FOR_SPECS" --all-controls 2>/dev/null)

if [ "${#SCAN_CTLS[@]}" -eq 0 ]; then
    echo "UNMEASURED  門から対照の一覧を取れなかった: $GATE_FOR_SPECS"
    echo '            --all-controls の口を消した/壊したなら、此処の呼び出しも同じ commit で直す事'
    echo "--- 合計: green=0 red=0 未測定=1(照合の網が空)---"
    exit 2
fi

for _f in "${SCAN_CTLS[@]}"; do
    [ -f "$_f" ] || continue
    case " ${LOCAL_CTLS[*]} ${EDITH_CTLS[*]} ${EXCLUDED_CTLS[*]:-} " in
        *" $_f "*) : ;;
        *) echo "UNREG  $_f  ← どの一覧にも無い = **一度も回らない対照**"
           echo "         直し方: LOCAL_CTLS へ足す(既定)か、EXCLUDED_CTLS へ**理由付きで**入れる"
           red=$((red+1)); red_names+=("$(basename "$_f")(未登録)") ;;
    esac
done

for c in "${list[@]}"; do
    if [ ! -f "$c" ]; then
        echo "MISSING  $c  ← 書類が指しているのに無い"
        red=$((red+1)); red_names+=("$c(不在)")
        continue
    fi
    t0=$(date +%s)
    out="$(bash "$c" 2>&1)"; rc=$?
    t1=$(date +%s)
    last="$(echo "$out" | tail -1)"
    case "$rc" in
        0) green=$((green+1));      printf 'GREEN  %-34s %3ds  %s\n' "$(basename "$c")" "$((t1-t0))" "$last" ;;
        2) unmeasured=$((unmeasured+1)); unm_names+=("$(basename "$c")")
           printf 'UNMEA  %-34s %3ds  %s\n' "$(basename "$c")" "$((t1-t0))" "$last" ;;
        *) red=$((red+1)); red_names+=("$(basename "$c")")
           printf 'RED    %-34s %3ds  %s\n' "$(basename "$c")" "$((t1-t0))" "$last"
           echo "$out" | sed 's/^/         /' | tail -12 ;;
    esac
done

echo ""
# ★除外の本数は**配列から数える**。手で書いた数は必ず腐る —— 実測 2026-08-07:
#   `EDITH_CTLS` は3本在るのに此処は「2本」と刷っていて、門の一覧(65)と掃引の出力(62)を
#   突き合わせるまで誰も気付かなかった。1本が黙って落ちているのと見分けが付かない。
echo "RUN-CONTROLS: green=$green red=$red 未測定=$unmeasured  (対象 ${#list[@]}本$([ "$ALL" -eq 1 ] || echo "、edith専用${#EDITH_CTLS[@]}本は除外"))"
[ "$red" -gt 0 ] && echo "  赤: ${red_names[*]}"
[ "$unmeasured" -gt 0 ] && echo "  未測定(緑ではない): ${unm_names[*]} ← 条件が揃ってから回し直す事"

# ★三値を**終了コードでも**保つ(2026-08-03 に直した)。
#   直す前は `exit $(( red > 0 ))` で、未測定が在っても **0 = 緑**を返していた。
#   上の行で人には「未測定(緑ではない)」と正しく出しているのに、機械には緑と言う ——
#   §7-P-g と同じ族(判定は正しく作れていて、持ち帰る所で捨てる)の8件目。
#   この lane では**変異の走行中に `mutation-verdict` / `mutation-freeze` が 2 を返す**ので、
#   一番よく起きる状況で「一番重い対照2本を測っていない run」が緑を返していた事になる。
#   ★HANDOFF:2496 は最初から「0=緑 / 1=赤 / 2=測っていない の三値を保つ事」と書いてある。
#     規則は在った。**回す物が無かった** = DESIGN (19)。対照 = test/run-controls-controls.sh
if [ "$red" -gt 0 ]; then exit 1; fi
if [ "$unmeasured" -gt 0 ]; then exit 2; fi
exit 0
